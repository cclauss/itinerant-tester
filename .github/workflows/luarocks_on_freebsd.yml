name: FreeBSD Lua Test

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  freebsd-test:
    runs-on: ubuntu-latest
    name: Test on FreeBSD
    steps:
      - uses: actions/checkout@v7

      - name: Run in FreeBSD VM
        uses: vmactions/freebsd-vm@v1
        with:
          release: "14.1"
          usesh: true
          prepare: |
            pkg install -y lua51-luarocks
          run: |
            lua51 -v
            luarocks51 --version
            luarocks51 || true
            pkg install -y lua55
            luarocks51 --lua-version=5.5 --version
            luarocks51 --lua-version=5.5
