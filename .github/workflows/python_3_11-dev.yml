name: python_3_11-dev
on:
  workflow_dispatch:
jobs:
  python_3_11-dev:
    strategy:
      fail-fast: false
      matrix:
        package:
          - cchardet
          - frozenlist
          - yarl
          - git+https://github.com/PyYoshi/cChardet.git
          - git+https://github.com/aio-libs/frozenlist
          - git+https://github.com/aio-libs/yarl.git
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-python@v4
        with:
          python-version: 3.11-dev
      - run: pip install --upgrade pip
      - run: pip install ${{ matrix.package }}
