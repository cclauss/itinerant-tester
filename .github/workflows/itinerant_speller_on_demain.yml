name: itinerant_speller
on:
  workflow_dispatch:
    inputs:
      repo:
        description: 'Repo to spellcheck (user_name/repo_name)'
        required: true
jobs:
  itinerant_speller:
    runs-on: ubuntu-latest
    steps:
      - run: pip install codespell
      - run: echo "Spellchecking ${{ github.event.inputs.repo }}..."
      - run: git clone https://github.com/${{ github.event.inputs.repo }}
      - run: codespell --count --skip="*.po" || true  # --ignore-words-list="" --skip="*.css,*.js,*.json,*.lock,*.map,*.po,*.svg"
