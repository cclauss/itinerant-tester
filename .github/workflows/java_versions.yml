name: java_versions
on:
  workflow_dispatch:

jobs:
  java_versions:
    strategy:
      fail-fast: false
      matrix:
        java-version: ['8', '11', '21']
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-java@v2  # not v4 for Java 8
        with:
          distribution: 'microsoft'
          java-version: ${{ matrix.java-version }}
      - run: java --version
