group: travis_latest
dist: xenial  # required for Python >= 3.7 (travis-ci/travis-ci#9069)
language: python
cache: pip
python:
  #- 2.7
  #- 3.5
  #- 3.6
  - 3.7
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
  - REPO=jaraco/backports.datetime_timestamp  # has .travis.yml file
  - REPO=jaraco/backports.pdb  # has .travis.yml file
  - REPO=jaraco/cmdix  # has .travis.yml file
  - REPO=jaraco/configparser  # has .travis.yml file
  - REPO=jaraco/cpython  # has .travis.yml file
  - REPO=jaraco/deepdiff  # has .travis.yml file
  - REPO=jaraco/irc  # has .travis.yml file
  - REPO=jaraco/jaraco.collections  # has .travis.yml file
  - REPO=jaraco/jaraco.compat  # has .travis.yml file
  - REPO=jaraco/jaraco.functools  # has .travis.yml file
  - REPO=jaraco/jaraco.itertools  # has .travis.yml file
  - REPO=jaraco/jaraco.logging  # has .travis.yml file
  - REPO=jaraco/jaraco.mongodb  # has .travis.yml file
  - REPO=jaraco/jaraco.net  # has .travis.yml file
  - REPO=jaraco/jaraco.text  # has .travis.yml file
  - REPO=jaraco/jaraco.tidelift  # has .travis.yml file
  - REPO=jaraco/keyring  # has .travis.yml file
  - REPO=jaraco/mongo-python-driver  # has .travis.yml file
  - REPO=jaraco/path.py  # has .travis.yml file
  - REPO=jaraco/peps  # has .travis.yml file
  - REPO=jaraco/pip-run  # has .travis.yml file
  - REPO=jaraco/pipx  # has .travis.yml file
  - REPO=jaraco/portend  # has .travis.yml file
  - REPO=jaraco/pyjwt  # has .travis.yml file
  - REPO=jaraco/pytest-black  # has .travis.yml file
  - REPO=jaraco/pytest-black-multipy  # has .travis.yml file
  - REPO=jaraco/skeleton  # has .travis.yml file
  - REPO=jaraco/svg.charts  # has .travis.yml file
  - REPO=jaraco/tempora  # has .travis.yml file
  - REPO=jaraco/zipp  # has .travis.yml file
install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E9,F63,F72,F82 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
