group: travis_latest
# dist: xenial  # required for Python 3.7
# sudo: true    # required for Python 3.7
language: python
env:
    - REPO=amercader/MapFish-Print-IIS
    - REPO=amercader/OWSLib
    - REPO=amercader/ckan  # has .travis.yml file
    - REPO=amercader/ckanext-adfs
    - REPO=amercader/ckanext-datajson
    - REPO=amercader/ckanext-datarequests
    - REPO=amercader/ckanext-harvest
    - REPO=amercader/ckanext-ldap
    - REPO=amercader/ckanext-rq
    - REPO=amercader/ckanext-scheming  # has .travis.yml file
    - REPO=amercader/datapusher  # has .travis.yml file
    - REPO=amercader/docker-ckan
    - REPO=amercader/example-docker-python
    - REPO=amercader/flask-babel  # has .travis.yml file
    - REPO=amercader/geojson-diff-examples
    - REPO=amercader/gini-index
    - REPO=amercader/hashtag-listener
    - REPO=amercader/ne-data-test
    - REPO=amercader/newcastle-libraries-data
    - REPO=amercader/pycsw  # has .travis.yml file
    - REPO=amercader/registry
    - REPO=amercader/requests  # has .travis.yml file
    - REPO=amercader/s-and-p-500-companies  # has .travis.yml file
    - REPO=amercader/test_travis  # has .travis.yml file
    - REPO=amercader/validate-sdg-dp
    - REPO=amercader/weberror
    - REPO=amercader/wikigeolinks

cache: pip
python:
    #- 2.7
    - 3.6
    #- nightly
    #- pypy
    #- pypy3
matrix:
    allow_failures:
        - 2.7
        - python: nightly
        - python: pypy
        - python: pypy3
install:
    #- pip install -r requirements.txt
    - pip install flake8  # pytest  # add other testing frameworks later
before_script:
    - URL=https://github.com/${REPO}
    - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
    - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
    - cd ~/${REPO}
script:
    - echo stop the build if there are Python syntax errors or undefined names
    - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
    - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
    - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
    - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
    #- true  # pytest --capture=sys
notifications:
    on_success: change
    on_failure: change  # `always` will be the setting once code changes slow down
