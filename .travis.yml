group: travis_latest
dist: xenial  # required for Python >= 3.7 (travis-ci/travis-ci#9069)
language: python
cache: pip
python:
  #- 2.7
  #- 3.5
  #- 3.6
  - 3.7
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
  - REPO=jayvdb/asciimatics  # has .travis.yml file
  - REPO=jayvdb/coverage_config_reload_plugin  # has .travis.yml file
  - REPO=jayvdb/coverage_env_plugin  # has .travis.yml file
  - REPO=jayvdb/django-compat-patcher
  - REPO=jayvdb/django-cors-headers  # has .travis.yml file
  - REPO=jayvdb/django-extensions  # has .travis.yml file
  - REPO=jayvdb/django-invitations  # has .travis.yml file
  - REPO=jayvdb/django-rest-auth  # has .travis.yml file
  - REPO=jayvdb/django-rest-knox  # has .travis.yml file
  - REPO=jayvdb/django-rest-social-auth  # has .travis.yml file
  - REPO=jayvdb/exif-py  # has .travis.yml file
  - REPO=jayvdb/flower  # has .travis.yml file
  - REPO=jayvdb/hyper  # has .travis.yml file
  - REPO=jayvdb/idna-ssl  # has .travis.yml file
  - REPO=jayvdb/ijson  # has .travis.yml file
  - REPO=jayvdb/jwt-oauth2
  - REPO=jayvdb/minio-py  # has .travis.yml file
  - REPO=jayvdb/pew  # has .travis.yml file
  - REPO=jayvdb/pipenv
  - REPO=jayvdb/plotly.py
  - REPO=jayvdb/pypistats  # has .travis.yml file
  - REPO=jayvdb/python-quilt  # has .travis.yml file
  - REPO=jayvdb/pythonz
  - REPO=jayvdb/requirementslib  # has .travis.yml file
  - REPO=jayvdb/resumable-urlretrieve  # has .travis.yml file
  - REPO=jayvdb/rpm-py-installer  # has .travis.yml file
  - REPO=jayvdb/social-core  # has .travis.yml file
  - REPO=jayvdb/tablib  # has .travis.yml file
  - REPO=jayvdb/thumbor  # has .travis.yml file
  - REPO=jayvdb/tox_appveyor

install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=4.x
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E9,F63,F72,F82 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
