group: travis_latest
# dist: xenial  # required for Python 3.7
# sudo: true    # required for Python 3.7
language: python
env:
    - REPO=DistrictDataLabs/02-OKJK
    - REPO=DistrictDataLabs/02-labormatch
    - REPO=DistrictDataLabs/02-seefish
    - REPO=DistrictDataLabs/02-synthesizers
    - REPO=DistrictDataLabs/03-EMU
    - REPO=DistrictDataLabs/03-gentrifuge
    - REPO=DistrictDataLabs/04-team2
    - REPO=DistrictDataLabs/DP-Tutorial
    - REPO=DistrictDataLabs/baleen  # has .travis.yml file
    - REPO=DistrictDataLabs/bigtooth
    - REPO=DistrictDataLabs/blog-files
    - REPO=DistrictDataLabs/calendar
    - REPO=DistrictDataLabs/chirpy
    - REPO=DistrictDataLabs/dedupe-examples
    - REPO=DistrictDataLabs/django-data-product
    - REPO=DistrictDataLabs/entity-resolution
    - REPO=DistrictDataLabs/group-d
    - REPO=DistrictDataLabs/intro-to-python
    - REPO=DistrictDataLabs/logbook  # has .travis.yml file
    - REPO=DistrictDataLabs/mapreduce
    - REPO=DistrictDataLabs/minimum-entropy  # has .travis.yml file
    - REPO=DistrictDataLabs/minke  # has .travis.yml file
    - REPO=DistrictDataLabs/partisan-discourse  # has .travis.yml file
    - REPO=DistrictDataLabs/product-classifier  # has .travis.yml file
    - REPO=DistrictDataLabs/science-bookclub
    - REPO=DistrictDataLabs/semnet-similarity  # has .travis.yml file
    - REPO=DistrictDataLabs/spark-workshop
    - REPO=DistrictDataLabs/yellowbrick  # has .travis.yml file
    - REPO=DistrictDataLabs/yellowbrick-docs-tr
    - REPO=DistrictDataLabs/yellowbrick-docs-zh
cache: pip
python:
    #- 2.7
    - 3.6
    #- nightly
    #- pypy
    #- pypy3
matrix:
    allow_failures:
        - 2.7
        - python: nightly
        - python: pypy
        - python: pypy3
install:
    #- pip install -r requirements.txt
    - pip install flake8  # pytest  # add other testing frameworks later
before_script:
    - URL=https://github.com/${REPO}
    - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
    - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
    - cd ~/${REPO}
script:
    - echo stop the build if there are Python syntax errors or undefined names
    - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
    - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
    - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
    - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
    #- true  # pytest --capture=sys
notifications:
    on_success: change
    on_failure: change  # `always` will be the setting once code changes slow down
