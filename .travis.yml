group: travis_latest
# dist: xenial  # required for Python 3.7
# sudo: true    # required for Python 3.7
language: python
env:
    - REPO=apache/allura  # has .travis.yml file
    - REPO=apache/bloodhound
    - REPO=apache/cassandra-dtest  # has .travis.yml file
    - REPO=apache/chemistry-cmislib
    - REPO=apache/cloudstack-docs-rn
    - REPO=apache/comdev-reporter
    - REPO=apache/couchdb-documentation  # has .travis.yml file
    - REPO=apache/fluo-muchos  # has .travis.yml file
    - REPO=apache/incubator-airflow  # has .travis.yml file
    - REPO=apache/incubator-airflow-old  # has .travis.yml file
    - REPO=apache/incubator-ariatosca  # has .travis.yml file
    - REPO=apache/incubator-mxnet  # has .travis.yml file
    - REPO=apache/incubator-openwhisk-deploy-mesos  # has .travis.yml file
    - REPO=apache/incubator-openwhisk-utilities  # has .travis.yml file
    - REPO=apache/incubator-pagespeed-drp
    - REPO=apache/incubator-sdap-edge
    - REPO=apache/incubator-sdap-nexus
    - REPO=apache/incubator-sdap-nexusproto
    - REPO=apache/incubator-sdap-ningesterpy
    - REPO=apache/incubator-senssoft-distill
    - REPO=apache/incubator-spot
    - REPO=apache/incubator-warble-node
    - REPO=apache/incubator-wave-docs
    - REPO=apache/infrastructure-puppet
    - REPO=apache/kibble
    - REPO=apache/kibble-scanners
    - REPO=apache/libcloud
    - REPO=apache/predictionio-sdk-python  # has .travis.yml file
    - REPO=apache/qpid-interop-test
    - REPO=apache/qpid-python

cache: pip
python:
    #- 2.7
    - 3.6
    #- nightly
    #- pypy
    #- pypy3
matrix:
    allow_failures:
        - 2.7
        - python: nightly
        - python: pypy
        - python: pypy3
install:
    #- pip install -r requirements.txt
    - pip install flake8  # pytest  # add other testing frameworks later
before_script:
    - URL=https://github.com/${REPO}
    - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
    - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
    - cd ~/${REPO}
script:
    - echo stop the build if there are Python syntax errors or undefined names
    - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
    - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
    - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
    - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
    #- true  # pytest --capture=sys
notifications:
    on_success: change
    on_failure: change  # `always` will be the setting once code changes slow down
