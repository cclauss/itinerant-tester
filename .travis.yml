group: travis_latest
dist: xenial  # required for Python >= 3.7 (travis-ci/travis-ci#9069)
language: python
cache: pip
python:
  #- 2.7
  #- 3.5
  #- 3.6
  - 3.7
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
  - REPO=ColdGrub1384/Pyto
  - REPO=HelloZeroNet/ZeroNet
  - REPO=Helpsypoo/primer
  - REPO=MVIG-SJTU/AlphaPose
  - REPO=Netflix/bless
  - REPO=Netflix/photon
  - REPO=SecureAuthCorp/impacket
  - REPO=TheAlgorithms/Python
  - REPO=TheKingOfDuck/fuzzDicts
  - REPO=WenjieZ/TSCV
  - REPO=achael/eht-imaging
  - REPO=achaiah/pywick
  - REPO=alicevision/meshroom
  - REPO=apache/beam
  - REPO=apachecn/AiLearning
  - REPO=cclauss/Pyto-module-versions
  - REPO=cclauss/pythonista-module-versions
  - REPO=cvpe/Pythonista-scripts
  - REPO=druid-io/pydruid
  - REPO=edx/codejail
  - REPO=edx/edx-search
  - REPO=edx/edx-val
  - REPO=electron/electron
  - REPO=getredash/redash
  - REPO=gmjosack/mrproxy
  - REPO=ibm-watson-iot/functions
  - REPO=ibmdb/python-ibmdb
  - REPO=johanjanssen/ChocolateyPackages
  - REPO=jython/book
  - REPO=kaushaltrivedi/fast-bert
  - REPO=keras-team/keras
  - REPO=kubeflow/kubeflow
  - REPO=libkeepass/libkeepass
  - REPO=nodejs/TSC
  - REPO=nodejs/build
  - REPO=nodejs/code-and-learn
  - REPO=nodejs/moderation
  - REPO=nodejs/node
  - REPO=nodejs/node-chakracore
  - REPO=nodejs/node-gyp
  - REPO=nodejs/node-v8
  - REPO=o-lim/generate-ninja
  - REPO=omz/Pythonista-Issues
  - REPO=proudcanadianeh/ChocoPackages
  - REPO=pyca/pyopenssl
  - REPO=pypa/pip
  - REPO=python/typed_ast
  - REPO=pytries/datrie
  - REPO=revalo/pishot
  - REPO=spotify/snakebite
  - REPO=tensorflow/probability
  - REPO=thunlp/ERNIE
  - REPO=tiangolo/meinheld-gunicorn-flask-docker
  - REPO=wandb/client
  - REPO=williamjacksn/python-rainwave-client
  - REPO=xonsh/xonsh  
install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E9,F63,F72,F82 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
