group: travis_latest
dist: xenial  # required for Python >= 3.7 (travis-ci/travis-ci#9069)
language: python
cache: pip
python:
  #- 2.7
  #- 3.5
  #- 3.6
  - 3.7
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
  - REPO=3b1b/manim
  - REPO=AI-DI/Brancher
  - REPO=AlessandroZ/LaZagne
  - REPO=Azure/hpcpack-acm-api-python
  - REPO=Azure/magic-module-specs
  - REPO=Azure/msrest-for-python
  - REPO=ColdGrub1384/Pyto
  - REPO=ConvLab/ConvLab
  - REPO=CorentinJ/Real-Time-Voice-Cloning
  - REPO=GoogleCloudPlatform/cloudml-samples
  - REPO=Hellowlol/bw_plex
  - REPO=Homebrew/homebrew-cask
  - REPO=IBM/Java-MicroProfile-on-Kubernetes
  - REPO=IBM/ibm-cos-sdk-js
  - REPO=IBM/ibm-cos-sdk-python-s3transfer
  - REPO=IBM/spring-boot-microservices-on-kubernetes
  - REPO=IBM/watson-banking-chatbot
  - REPO=InsaneLife/ChineseNLPCorpus
  - REPO=LazoCoder/Pokemon-Terminal
  - REPO=SlavikMIPT/tgcloud
  - REPO=TheAlgorithms/Python
  - REPO=Threezh1/JSFinder
  - REPO=Zucccs/PhoneSploit
  - REPO=al0ne/Vxscan
  - REPO=cardinal-dev/Cardinal
  - REPO=catmaid/CATMAID
  - REPO=cclauss/Find-Python-syntax-errors-action
  - REPO=cclauss/Pyto-module-versions
  - REPO=cclauss/pythonista-module-versions
  - REPO=ckan/ckan
  - REPO=dask/partd
  - REPO=devAmoghS/Python-Interview-Problems-for-Practice
  - REPO=garethdmm/gryphon
  - REPO=ggtracker/sc2reader
  - REPO=git/git
  - REPO=humberry/PythonCheatSheet
  - REPO=internetarchive/openlibrary
  - REPO=ipython/traitlets
  - REPO=jhjacobsen/invertible-resnet
  - REPO=jorisroovers/gitlint
  - REPO=koalaman/shellcheck
  - REPO=matrix-org/synapse
  - REPO=momosecurity/aswan
  - REPO=nccgroup/ScoutSuite
  - REPO=nodejs/TSC
  - REPO=nodejs/build
  - REPO=nodejs/members
  - REPO=nodejs/moderation
  - REPO=nodejs/node
  - REPO=nodejs/node-chakracore
  - REPO=nodejs/node-gyp
  - REPO=nodejs/node-v8
  - REPO=nodejs/nodejs.org
  - REPO=nodejs/reliability
  - REPO=omz/Pythonista-Issues
  - REPO=postgis/postgis
  - REPO=pypa/pip
  - REPO=rwightman/pytorch-image-models
  - REPO=tensorflow/text
  - REPO=tensorflow/transform
  - REPO=torvalds/linux
  - REPO=wangzheng0822/algo
  - REPO=watson-developer-cloud/java-sdk
  - REPO=watson-developer-cloud/tone-analyzer-nodejs
  - REPO=xcp-ng/xcp
  - REPO=zihangdai/xlnet
  - REPO=zuoym15/craves.ai

install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E9,F63,F72,F82 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
