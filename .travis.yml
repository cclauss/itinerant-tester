group: travis_latest
dist: xenial    # required for Python 3.7 (travis-ci/travis-ci#9069)
sudo: required  # required for Python 3.7 (travis-ci/travis-ci#9069)
language: python
cache: pip
python:
  #- 2.7
  #- 3.4
  #- 3.5
  #- 3.6
  - 3.7
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
    - REPO=Swall0w/BBox-Label-Tool
    - REPO=Swall0w/DeepEmbeddedClustering
    - REPO=Swall0w/Detectron.pytorch  # has .travis.yml file
    - REPO=Swall0w/ImageMarker
    - REPO=Swall0w/MachineLearningAPI
    - REPO=Swall0w/PodcastStreamer
    - REPO=Swall0w/PyTorch-YOLOv3
    - REPO=Swall0w/PytorchSSD
    - REPO=Swall0w/arxiv-bot  # has .travis.yml file
    - REPO=Swall0w/chainer-crnn
    - REPO=Swall0w/chainer-mask-rcnn  # has .travis.yml file
    - REPO=Swall0w/clib  # has .travis.yml file
    - REPO=Swall0w/crnn.pytorch
    - REPO=Swall0w/dual-path-networks
    - REPO=Swall0w/image-captioning
    - REPO=Swall0w/learning-nonparametric-bayesian
    - REPO=Swall0w/maskrcnn-benchmark
    - REPO=Swall0w/matching-networks-pytorch
    - REPO=Swall0w/mchainercv  # has .travis.yml file
    - REPO=Swall0w/microservice-ml-demo
    - REPO=Swall0w/protobuf-to-dict
    - REPO=Swall0w/pytorch-mask-rcnn
    - REPO=Swall0w/pytorch-mobilenet-v2
    - REPO=Swall0w/pytorch-tutorial
    - REPO=Swall0w/pytorch-yolo-v3
    - REPO=Swall0w/pytorch-yolo3
    - REPO=Swall0w/siamese-network
    - REPO=Swall0w/textboxes
    - REPO=Swall0w/torchstat
    - REPO=Swall0w/word2vec
install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
