group: travis_latest
dist: xenial  # required for Python >= 3.7 (travis-ci/travis-ci#9069)
language: python
cache: pip
python:
  #- 2.7
  #- 3.4
  #- 3.5
  #- 3.6
  - 3.7
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
    - REPO=tonybaloney/StackStorm-infoblox
    - REPO=tonybaloney/baloneyscript
    - REPO=tonybaloney/cards  # has .travis.yml file
    - REPO=tonybaloney/cpython  # has .travis.yml file
    - REPO=tonybaloney/django-badger  # has .travis.yml file
    - REPO=tonybaloney/email-toolbox
    - REPO=tonybaloney/friendly-deprecation-test
    - REPO=tonybaloney/infoblox-client  # has .travis.yml file
    - REPO=tonybaloney/libcloud
    - REPO=tonybaloney/pathgather  # has .travis.yml file
    - REPO=tonybaloney/performance  # has .travis.yml file
    - REPO=tonybaloney/pip  # has .travis.yml file
    - REPO=tonybaloney/pluralsight  # has .travis.yml file
    - REPO=tonybaloney/pytest  # has .travis.yml file
    - REPO=tonybaloney/python-packaging-user-guide  # has .travis.yml file
    - REPO=tonybaloney/python-zeep  # has .travis.yml file
    - REPO=tonybaloney/pyviera-1
    - REPO=tonybaloney/requests-staticmock  # has .travis.yml file
    - REPO=tonybaloney/retox  # has .travis.yml file
    - REPO=tonybaloney/salt
    - REPO=tonybaloney/sciencelogic  # has .travis.yml file
    - REPO=tonybaloney/search-tweets-python
    - REPO=tonybaloney/sphinxcontrib-confluencebuilder  # has .travis.yml file
    - REPO=tonybaloney/stackstorm-infoblox-1
    - REPO=tonybaloney/stackstorm-servicenow
    - REPO=tonybaloney/tox-nuitka  # has .travis.yml file
    - REPO=tonybaloney/tox-pipenv  # has .travis.yml file
    - REPO=tonybaloney/wily  # has .travis.yml file
    - REPO=tonybaloney/woof
    - REPO=tonybaloney/workday  # has .travis.yml file

install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
