group: travis_latest
dist: xenial  # required for Python >= 3.7 (travis-ci/travis-ci#9069)
language: python
cache: pip
python:
  #- 2.7
  #- 3.4
  #- 3.5
  #- 3.6
  - 3.7
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
  - REPO=Pylons/colander  # has .travis.yml file
  - REPO=Pylons/deformdemo  # has .travis.yml file
  - REPO=Pylons/docs-style-guide  # has .travis.yml file
  - REPO=Pylons/hupper  # has .travis.yml file
  - REPO=Pylons/paginate  # has .travis.yml file
  - REPO=Pylons/pastedeploy  # has .travis.yml file
  - REPO=Pylons/peppercorn  # has .travis.yml file
  - REPO=Pylons/plaster  # has .travis.yml file
  - REPO=Pylons/plaster_pastedeploy  # has .travis.yml file
  - REPO=Pylons/pyramid  # has .travis.yml file
  - REPO=Pylons/pyramid-bundling-example
  - REPO=Pylons/pyramid-cookiecutter-alchemy  # has .travis.yml file
  - REPO=Pylons/pyramid-cookiecutter-starter  # has .travis.yml file
  - REPO=Pylons/pyramid-cookiecutter-zodb  # has .travis.yml file
  - REPO=Pylons/pyramid_beaker
  - REPO=Pylons/pyramid_blogr
  - REPO=Pylons/pyramid_cookbook  # has .travis.yml file
  - REPO=Pylons/pyramid_debugtoolbar  # has .travis.yml file
  - REPO=Pylons/pyramid_jinja2  # has .travis.yml file
  - REPO=Pylons/pyramid_mailer  # has .travis.yml file
  - REPO=Pylons/pyramid_retry  # has .travis.yml file
  - REPO=Pylons/pyramid_tm  # has .travis.yml file
  - REPO=Pylons/pyramid_tutorials
  - REPO=Pylons/pyramid_zcml  # has .travis.yml file
  - REPO=Pylons/substanced  # has .travis.yml file
  - REPO=Pylons/venusian  # has .travis.yml file
  - REPO=Pylons/waitress  # has .travis.yml file
  - REPO=Pylons/webob  # has .travis.yml file
  - REPO=Pylons/webtest  # has .travis.yml file
  - REPO=Pylons/zodburi  # has .travis.yml file
install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
