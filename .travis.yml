group: travis_latest
os: linux
dist: focal
language: python
cache: pip
python:
  #- 2.7
  #- 3.5
  #- 3.6
  #- 3.7
  - 3.8
jobs:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
  - REPO=MarioVilas/burp-retire-js    # 2018-03-26 h3xstream/burp-retire-js
  - REPO=h3xstream/burp-retire-js
  - REPO=MarioVilas/cve_wordpress     # 2014-09-25
  - REPO=MarioVilas/forensic_tools    # archived
  - REPO=MarioVilas/frankencert       # 2017-03-28 sumanj/frankencert
  - REPO=sumanj/frankencert
  - REPO=MarioVilas/FuzzLabs          # 2016-04-17 jack51706/FuzzLabs
  - REPO=jack51706/FuzzLabs
  - REPO=MarioVilas/googlesearch      # 2020-07-11
  - REPO=MarioVilas/kitchensink       # 2020-02-24
  - REPO=MarioVilas/ncn2k14_ctf       # 2014-11-07 jroimartin/ncn2k14_ctf
  - REPO=jroimartin/ncn2k14_ctf
  - REPO=MarioVilas/rtsp-fuzzer       # 2013-07-19
  - REPO=MarioVilas/secondlife-experiments  # 2013-07-19
  - REPO=MarioVilas/shellgen          # 2017-11-26
  - REPO=MarioVilas/socksipy-branch   # 2015-03-26
  - REPO=MarioVilas/theHarvester      # 2018-07-07 laramies/theHarvester
  - REPO=laramies/theHarvester
  - REPO=MarioVilas/thetick           # 2019-09-27 nccgroup/thetick
  - REPO=nccgroup/thetick
  - REPO=MarioVilas/url_shortener_utils  # 2014-03-19
  - REPO=MarioVilas/vtrace-mirror     # 2015-03-26
  - REPO=MarioVilas/winappdbg         # 2020-04-15
  - REPO=MarioVilas/write-ups         # 2014-11-10 ctfs/write-ups-2014
  - REPO=ctfs/write-ups-2014
install:
  #- pip install -r requirements.txt
  - pip install codespell flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  - codespell . --ignore-words-list=ba,referer --skip=./.* --quiet-level=2 || true
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
