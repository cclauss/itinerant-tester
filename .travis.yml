group: travis_latest
dist: xenial  # required for Python >= 3.7 (travis-ci/travis-ci#9069)
language: python
cache: pip
python:
  #- 2.7
  #- 3.5
  #- 3.6
  - 3.7
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
  - REPO=CODAIT/node-red-contrib-model-asset-exchange
  - REPO=codait/MAX-Facial-Age-Estimator-Mini-Web-App  # has .travis.yml file
  - REPO=codait/graph_def_editor
  - REPO=codait/max-base
  - REPO=IBM/FfDL
  - REPO=IBM/max-developer-tutorial
  - REPO=IBM/MAX-Base
  - REPO=IBM/AIF360  # has .travis.yml file
  - REPO=IBM/HackerOne-Notifications-Events-via-IBM-Cloud-Slack
  - REPO=IBM/MAX-Breast-Cancer-Mitosis-Detector  # has .travis.yml file
  - REPO=IBM/MAX-Facial-Age-Estimator  # has .travis.yml file
  - REPO=IBM/MAX-Facial-Age-Estimator-Web-App  # has .travis.yml file
  - REPO=IBM/MAX-Facial-Emotion-Classifier  # has .travis.yml file
  - REPO=IBM/MAX-Fast-Neural-Style-Transfer  # has .travis.yml file
  - REPO=IBM/MAX-Framework  # has .travis.yml file
  - REPO=IBM/MAX-Image-Colorizer  # has .travis.yml file
  - REPO=IBM/MAX-Image-Completer  # has .travis.yml file
  - REPO=IBM/MAX-Inception-ResNet-v2  # has .travis.yml file
  - REPO=IBM/MAX-Named-Entity-Tagger  # has .travis.yml file
  - REPO=IBM/MAX-News-Text-Generator  # has .travis.yml file
  - REPO=IBM/MAX-Object-Detector  # has .travis.yml file
  - REPO=IBM/MAX-ResNet-50  # has .travis.yml file
  - REPO=IBM/MAX-Review-Text-Generator  # has .travis.yml file
  - REPO=IBM/MAX-Scene-Classifier  # has .travis.yml file
  - REPO=IBM/castor-messaging
  - REPO=IBM/django-web-app
  - REPO=IBM/ibm-block-csi-driver
  - REPO=IBM/mi-prometheus
  - REPO=IBM/osslm-ansible-resource-manager
  - REPO=IBM/probabilistic-federated-neural-matching
  - REPO=IBM/python-itoolkit  # has .travis.yml file
  - REPO=IBM/python-sdk-core  # has .travis.yml file
  - REPO=IBM/pytorchpipe  # has .travis.yml file
  - REPO=IBM/stix-shifter  # has .travis.yml file
  - REPO=IBM/stomp
  - REPO=IBM/watson-assistant-workbench  # has .travis.yml file
  - REPO=IBM/watson-box-skills-with-nlu
  
install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E9,F63,F72,F82 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
