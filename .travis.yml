group: travis_latest
dist: xenial    # required for Python 3.7 (travis-ci/travis-ci#9069)
sudo: required  # required for Python 3.7 (travis-ci/travis-ci#9069)
language: python
cache: pip
python:
  #- 2.7
  #- 3.4
  #- 3.5
  #- 3.6
  - 3.7
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
    - REPO=jtauber/RocAlphaGo  # has .travis.yml file
    - REPO=jtauber/Thesaurus_Morphologicus_Linguae_Graecae
    - REPO=jtauber/a2disk
    - REPO=jtauber/apostolic-fathers
    - REPO=jtauber/applepy
    - REPO=jtauber/blockchain  # has .travis.yml file
    - REPO=jtauber/core-gnt-vocab
    - REPO=jtauber/depquery  # has .travis.yml file
    - REPO=jtauber/dux
    - REPO=jtauber/forth
    - REPO=jtauber/functional-differential-geometry  # has .travis.yml file
    - REPO=jtauber/gnt-accentuation
    - REPO=jtauber/graded-reader
    - REPO=jtauber/greek-accentuation  # has .travis.yml file
    - REPO=jtauber/greek-inflexion  # has .travis.yml file
    - REPO=jtauber/greek-reader  # has .travis.yml file
    - REPO=jtauber/greek-utils  # has .travis.yml file
    - REPO=jtauber/gtp  # has .travis.yml file
    - REPO=jtauber/inflexion  # has .travis.yml file
    - REPO=jtauber/learning-greek
    - REPO=jtauber/midiwrite
    - REPO=jtauber/oxlos2
    - REPO=jtauber/pygo
    - REPO=jtauber/pykanren
    - REPO=jtauber/pyuca  # has .travis.yml file
    - REPO=jtauber/quantum-workbench  # has .travis.yml file
    - REPO=jtauber/quantumpy  # has .travis.yml file
    - REPO=jtauber/sgf  # has .travis.yml file
    - REPO=jtauber/ultima4
    - REPO=jtauber/ultima6

install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
