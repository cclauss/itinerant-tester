group: travis_latest
os: linux
dist: focal
language: python
cache: pip
python:
  #- 2.7
  #- 3.5
  #- 3.6
  #- 3.7
  - 3.8
jobs:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
  - REPO=elastic/0bin                 # 2017-10-30 tylerjl/0bin
  - REPO=tylerjl/0bin
  - REPO=elastic/anonymize-it         # 2020-05-27
  - REPO=elastic/ansible-beats        # 2020-07-28
  - REPO=elastic/ansible-elasticsearch  # 2020-07-28
  - REPO=elastic/apm-agent-java       # 2020-08-14
  - REPO=elastic/apm-agent-python     # 2020-08-14
  - REPO=elastic/apm-agent-python-benchmarks  # 2019-11-25
  - REPO=elastic/apm-contrib          # 2020-06-25
  - REPO=elastic/apm-integration-testing  # 2020-08-04
  - REPO=elastic/apm-server           # 2020-08-14
  - REPO=elastic/app-search-python    # 2020-08-14
  - REPO=elastic/aws-mfa              # 2019-03-08 broamski/aws-mfa
  - REPO=broamski/aws-mfa
  - REPO=elastic/awscli-keyring       # 2016-07-07 sj26/awscli-keyring
  - REPO=sj26/awscli-keyring
  - REPO=elastic/beats                # 2020-08-14
  - REPO=elastic/beats-dashboards     # 2016-10-24
  - REPO=elastic/cloud-on-k8s         # 2020-08-14
  - REPO=elastic/code-examples_smoke  # 2019-08-14
  - REPO=elastic/connector-plugin-sdk  # 2019-01-07 tableau/connector-plugin-sdk
  - REPO=tableau/connector-plugin-sdk
  - REPO=elastic/ctags                # 2019-10-07 universal-ctags/ctags
  - REPO=universal-ctags/ctags
  - REPO=elastic/curator              # 2020-07-21
  - REPO=elastic/curator_api          # 2018-12-12
  - REPO=elastic/dcart                # 2019-12-19
  - REPO=elastic/detection-rules      # 2020-08-14
  - REPO=elastic/docs                 # 2020-08-14
  - REPO=elastic/ecs                  # 2020-08-14
  - REPO=elastic/ecs-logging-python   # 2020-08-04
  - REPO=elastic/eland                # 2020-08-14
  - REPO=elastic/elastic-charts       # 2020-08-11
  - REPO=elastic/elastic-stack-testing  # 2020-08-14
  - REPO=elastic/elasticboard         # 2017-04-04
  - REPO=elastic/elasticsearch        # 2020-08-14
  - REPO=elastic/elasticsearch-definitive-guide  # 2020-07-28
  - REPO=elastic/elasticsearch-dsl-py  # 2020-08-10
  - REPO=elastic/elasticsearch-lang-mvel  # 2019-01-16
  - REPO=elastic/elasticsearch-mapper-attachments  # 2019-05-03
  - REPO=elastic/elasticsearch-parent  # 2019-01-16
  - REPO=elastic/elasticsearch-plugins-script  # 2015-10-28
  - REPO=elastic/elasticsearch-py     # 2020-08-13
  - REPO=elastic/elasticsearch-py-async  # 2020-07-20
  - REPO=elastic/elasticsearch-river-couchdb  # 2019-01-16
  - REPO=elastic/elasticsearch-river-rabbitmq  # 2018-01-26
  - REPO=elastic/elasticsearch-river-twitter  # 2019-01-16
  - REPO=elastic/elasticsearch-river-wikipedia  # 2019-01-16
  - REPO=elastic/elasticsearch-sql-odbc  # 2020-07-27
  - REPO=elastic/elasticsearch-transport-memcached  # 2019-01-16
  - REPO=elastic/elasticsearch-transport-thrift  # 2019-01-16
  - REPO=elastic/elasticsearch-transport-wares  # 2019-01-16

install:
  #- pip install -r requirements.txt
  - pip install codespell flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  - codespell . --ignore-words-list=ba,referer --skip=./.* --quiet-level=2 || true
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
