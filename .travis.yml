group: travis_latest
language: python
env:
    - REPO=abingham/ACCUConf_Submission_Web_Application  # has .travis.yml file
    - REPO=abingham/ACCUConf_Website  # has .travis.yml file
    - REPO=abingham/RoxarAPI_Examples
    - REPO=abingham/aiohttp  # has .travis.yml file
    - REPO=abingham/ao-toolbox
    - REPO=abingham/appengine-test
    - REPO=abingham/asq  # has .travis.yml file
    - REPO=abingham/bespoke
    - REPO=abingham/brooks
    - REPO=abingham/categories-for-programmers
    - REPO=abingham/cython-sphinx-example
    - REPO=abingham/did-experiments
    - REPO=abingham/docopt-subcommands  # has .travis.yml file
    - REPO=abingham/flask-rest-notes
    - REPO=abingham/image_tool
    - REPO=abingham/ipytest
    - REPO=abingham/jupyter-elm-kernel
    - REPO=abingham/knowit-julekalendar-2015
    - REPO=abingham/npd-to-csv
    - REPO=abingham/pyqt-test
    - REPO=abingham/python-transducers  # has .travis.yml file
    - REPO=abingham/rope  # has .travis.yml file
    - REPO=abingham/runner_repl
    - REPO=abingham/segpy  # has .travis.yml file
    - REPO=abingham/spor  # has .travis.yml file
    - REPO=abingham/stringscore  # has .travis.yml file
    - REPO=abingham/traad  # has .travis.yml file
    - REPO=abingham/ultan  # has .travis.yml file
    - REPO=abingham/webassets-elm  # has .travis.yml file
    - REPO=abingham/zorkalike
cache: pip
python:
    - 2.7
    - 3.6
    #- nightly
    #- pypy
    #- pypy3
matrix:
    allow_failures:
        - python: nightly
        - python: pypy
        - python: pypy3
install:
    #- pip install -r requirements.txt
    - pip install flake8  # pytest  # add other testing frameworks later
before_script:
    - URL=https://github.com/${REPO}
    - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
    - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
    - cd ~/${REPO}
script:
    - echo stop the build if there are Python syntax errors or undefined names
    - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
    - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
    - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
    - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
    #- true  # pytest --capture=sys
notifications:
    on_success: change
    on_failure: change  # `always` will be the setting once code changes slow down
