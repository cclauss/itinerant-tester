group: travis_latest
dist: xenial  # required for Python >= 3.7 (travis-ci/travis-ci#9069)
language: python
cache: pip
python:
  #- 2.7
  #- 3.5
  #- 3.6
  - 3.7
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
  - REPO=ColdGrub1384/Pyto
  - REPO=FIWARE/dataModels
  - REPO=FreeCAD/FreeCAD-Doc
  - REPO=Homebrew/homebrew-cask
  - REPO=IBM/BlueMatter
  - REPO=IBM/CAST
  - REPO=IBM/Image-Captioning-Attack
  - REPO=IBM/MAX-Facial-Recognizer
  - REPO=IBM/MAX-Human-Pose-Estimator
  - REPO=IBM/MAX-Object-Detector
  - REPO=IBM/MAX-Word-Embedding-Generator
  - REPO=IBM/ZOO-Attack
  - REPO=IBM/data-broker
  - REPO=IBM/go-with-wakeup-profile
  - REPO=IBM/ibmpairs
  - REPO=IBM/kube-safe-scheduler
  - REPO=MODFLOW-USGS/modflow6
  - REPO=MODFLOW-USGS/modflow6-examples
  - REPO=MVIG-SJTU/AlphaPose
  - REPO=PyCQA/prospector
  - REPO=QuantStack/voila
  - REPO=TheAlgorithms/Python
  - REPO=Threezh1/JSFinder
  - REPO=Tribler/tribler
  - REPO=alpylmz/THE3TESTER
  - REPO=arcelien/pba
  - REPO=awslabs/gluon-ts
  - REPO=cclauss/pythonista-module-versions
  - REPO=ckan/ckan
  - REPO=cvpe/Pythonista-scripts
  - REPO=devAmoghS/Python-Interview-Problems-for-Practice
  - REPO=druid-io/pydruid
  - REPO=edx/edx-search
  - REPO=forseti-security/forseti-security
  - REPO=geoserver/geoserver
  - REPO=ggtracker/sc2reader
  - REPO=giampaolo/psutil
  - REPO=humberry/PythonCheatSheet
  - REPO=idaholab/Malcolm
  - REPO=kaushaltrivedi/fast-bert
  - REPO=kubernetes/kubernetes
  - REPO=marco-schmidt/am
  - REPO=matrix-org/synapse
  - REPO=nodejs/TSC
  - REPO=nodejs/build
  - REPO=nodejs/moderation
  - REPO=nodejs/node-chakracore
  - REPO=nodejs/node-gyp
  - REPO=nodejs/node-v8
  - REPO=nodejs/reliability
  - REPO=omz/Pythonista-Issues
  - REPO=peter0083/SPADE
  - REPO=postgis/postgis
  - REPO=pyenv/pyenv
  - REPO=python/devguide
  - REPO=semantic-release/semantic-release
  - REPO=tensorflow/tcav
  - REPO=tensorflow/text
  - REPO=vietnguyen91/Super-mario-bros-A3C-pytorch
  - REPO=watson-developer-cloud/app-insights-discovery
  - REPO=watson-developer-cloud/assistant-intermediate
  - REPO=watson-developer-cloud/java-sdk
  - REPO=watson-developer-cloud/tone-analyzer-nodejs
  - REPO=webpy/webpy
  
install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E9,F63,F72,F82 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
