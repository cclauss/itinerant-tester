group: travis_latest
dist: xenial  # required for Python >= 3.7 (travis-ci/travis-ci#9069)
language: python
cache: pip
python:
  #- 2.7
  #- 3.4
  #- 3.5
  #- 3.6
  - 3.7
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
    - REPO=THUFutureLab/gluon-face
    - REPO=payatu/CVE-2017-5638
    - REPO=payatu/CVE-2018-14442
    - REPO=payatu/Tiredful-API
    - REPO=payatu/ivr-pentest
    - REPO=s0md3v/Arjun
    - REPO=s0md3v/Blazy
    - REPO=s0md3v/Breacher
    - REPO=s0md3v/Cloak
    - REPO=s0md3v/Decodify
    - REPO=s0md3v/Entropy
    - REPO=s0md3v/Hash-Buster
    - REPO=s0md3v/JShell
    - REPO=s0md3v/Locky
    - REPO=s0md3v/Orbit
    - REPO=s0md3v/Photon  # has .travis.yml file
    - REPO=s0md3v/ReconDog  # has .travis.yml file
    - REPO=s0md3v/Shiva
    - REPO=s0md3v/Striker
    - REPO=s0md3v/XSStrike  # has .travis.yml file
    - REPO=s0md3v/Zen  # has .travis.yml file
    - REPO=s0md3v/hue
    - REPO=s0md3v/infinity
    - REPO=s0md3v/proxify
    - REPO=s0md3v/regxy
    - REPO=s0md3v/sqlmate
    - REPO=s0md3v/zetanize
    - REPO=home-assistant/appdaemon
    - REPO=home-assistant/example-custom-config
    - REPO=home-assistant/hass-release  # has .travis.yml file
    - REPO=home-assistant/hbmqtt-auth-home-assistant
    - REPO=home-assistant/home-assistant-cli  # has .travis.yml file
    - REPO=home-assistant/lambda-home-assistant-github
    - REPO=home-assistant/micropython-home-assistant
    - REPO=home-assistant/netdisco  # has .travis.yml file
    - REPO=home-assistant/pyharmony
    - REPO=home-assistant/python-openzwave  # has .travis.yml file
    - REPO=home-assistant/scenegen
    - REPO=home-assistant/warrant  # has .travis.yml file
    
install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
