group: travis_latest
dist: xenial  # required for Python >= 3.7 (travis-ci/travis-ci#9069)
language: python
cache: pip
python:
  #- 2.7
  #- 3.4
  #- 3.5
  #- 3.6
  - 3.7
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
  - REPO=pybee/Python-Windows-template
  - REPO=pybee/beefore
  - REPO=pybee/beekeeper
  - REPO=pybee/beeware
  - REPO=pybee/briefcase
  - REPO=pybee/briefcase-template
  - REPO=pybee/colosseum
  - REPO=pybee/cricket
  - REPO=pybee/duvet  # has .travis.yml file
  - REPO=pybee/legless-lizard
  - REPO=pybee/lektor-gravatar
  - REPO=pybee/liquid-demo
  - REPO=pybee/ouroboros  # has .travis.yml file
  - REPO=pybee/pytest-circleci
  - REPO=pybee/rubicon-objc  # has .travis.yml file
  - REPO=pybee/sealang  # has .travis.yml file
  - REPO=pybee/slick
  - REPO=pybee/tkreadonly
  - REPO=pybee/toga
  - REPO=pybee/toga-android
  - REPO=pybee/toga-cocoa
  - REPO=pybee/toga-core
  - REPO=pybee/toga-demo
  - REPO=pybee/toga-gtk
  - REPO=pybee/toga-iOS
  - REPO=pybee/toga_chart
  - REPO=pybee/travertino
  - REPO=pybee/voc
  - REPO=pybee/waggle
  - REPO=pybee/yorkshire4

install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E9,F63,F72,F82 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
