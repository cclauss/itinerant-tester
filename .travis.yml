group: travis_latest
dist: xenial  # required for Python >= 3.7 (travis-ci/travis-ci#9069)
language: python
cache: pip
python:
  #- 2.7
  #- 3.4
  #- 3.5
  #- 3.6
  - 3.7
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
  - REPO=kennethreitz/awesome-guides
  - REPO=kennethreitz/bash.py
  - REPO=kennethreitz/blindspin  # has .travis.yml file
  - REPO=kennethreitz/clint  # has .travis.yml file
  - REPO=kennethreitz/crayons
  - REPO=kennethreitz/delegator.py
  - REPO=kennethreitz/dj-database-url  # has .travis.yml file
  - REPO=kennethreitz/em-keyboard
  - REPO=kennethreitz/env  # has .travis.yml file
  - REPO=kennethreitz/flask-common
  - REPO=kennethreitz/flask-sslify
  - REPO=kennethreitz/grequests
  - REPO=kennethreitz/legit  # has .travis.yml file
  - REPO=kennethreitz/maya  # has .travis.yml file
  - REPO=kennethreitz/morepython.org
  - REPO=kennethreitz/pipenv-sublime
  - REPO=kennethreitz/pytheory
  - REPO=kennethreitz/records  # has .travis.yml file
  - REPO=kennethreitz/records-migrate
  - REPO=kennethreitz/requests  # has .travis.yml file
  - REPO=kennethreitz/requests-async  # has .travis.yml file
  - REPO=kennethreitz/requests3
  - REPO=kennethreitz/responder  # has .travis.yml file
  - REPO=kennethreitz/s3monkey
  - REPO=kennethreitz/saythanks.io
  - REPO=kennethreitz/setup.py
  - REPO=kennethreitz/showme
  - REPO=kennethreitz/std
  - REPO=kennethreitz/tablib  # has .travis.yml file
  - REPO=kennethreitz/twitter-scraper

install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E9,F63,F72,F82 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
