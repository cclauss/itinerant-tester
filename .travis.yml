group: travis_latest
dist: xenial  # required for Python >= 3.7 (travis-ci/travis-ci#9069)
language: python
cache: pip
python:
  #- 2.7
  #- 3.4
  #- 3.5
  #- 3.6
  - 3.7
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
    - REPO=nicolasnoble/CouchPotatoServer
    - REPO=nicolasnoble/node-pre-gyp-testcase
    - REPO=nicolasnoble/openssl-nodejs-ubuntu-demo
    - REPO=grpc/grpc-node
    - REPO=grpc/grpc-proto
    - REPO=ehashman/WelcomeBot
    - REPO=ehashman/ansiblefest
    - REPO=ehashman/auditwheel  # has .travis.yml file
    - REPO=ehashman/falcon-mutualauth  # has .travis.yml file
    - REPO=ehashman/molecule  # has .travis.yml file
    - REPO=ehashman/oh-bugimporters  # has .travis.yml file
    - REPO=ehashman/oh-mainline  # has .travis.yml file
    - REPO=ehashman/pelican-plugins  # has .travis.yml file
    - REPO=ehashman/peps  # has .travis.yml file
    - REPO=ehashman/pip  # has .travis.yml file
    - REPO=ehashman/pyceo
    - REPO=ehashman/rickastley
    - REPO=unode/Coherence
    - REPO=unode/cloud-genomics
    - REPO=unode/django-mailer  # has .travis.yml file
    - REPO=unode/ete  # has .travis.yml file
    - REPO=unode/firefox_decrypt  # has .travis.yml file
    - REPO=unode/htseq
    - REPO=unode/jug  # has .travis.yml file
    - REPO=unode/mOTUs_v2
    - REPO=unode/mOTUs_v2_deps
    - REPO=unode/nixs
    - REPO=unode/panyc  # has .travis.yml file
    - REPO=unode/pytagger
    - REPO=unode/python-feedparser
    - REPO=unode/q
    - REPO=unode/simpletap  # has .travis.yml file

install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
