group: travis_latest
# dist: xenial  # required for Python 3.7
# sudo: true    # required for Python 3.7
language: python
env:
    - REPO=Fematich/EMall
    - REPO=Fematich/Mediargus_code
    - REPO=Fematich/MilkBarrelDetector
    - REPO=Fematich/PyMongoStore
    - REPO=Fematich/RedNotebookPusher
    - REPO=Fematich/SleepHue
    - REPO=Fematich/TwitterAPI
    - REPO=Fematich/TwitterStream-store
    - REPO=Fematich/agenda_updater
    - REPO=Fematich/article_browser
    - REPO=Fematich/astrohack_flask
    - REPO=Fematich/cloudml-examples
    - REPO=Fematich/cnn-text-classification-tf
    - REPO=Fematich/conceptnetwork
    - REPO=Fematich/dataprocessing
    - REPO=Fematich/google-cloud-python  # has .travis.yml file
    - REPO=Fematich/incubator-airflow  # has .travis.yml file
    - REPO=Fematich/mlengine-boilerplate
    - REPO=Fematich/mongo_iterator
    - REPO=Fematich/muc4_proc
    - REPO=Fematich/nn_ner
    - REPO=Fematich/onco
    - REPO=Fematich/python-docs-samples  # has .travis.yml file
    - REPO=Fematich/rss_datamachina
    - REPO=Fematich/tftransform-demo
    - REPO=Fematich/word2vec_docrepr

cache: pip
python:
    #- 2.7
    - 3.6
    #- nightly
    #- pypy
    #- pypy3
matrix:
    allow_failures:
        - 2.7
        - python: nightly
        - python: pypy
        - python: pypy3
install:
    #- pip install -r requirements.txt
    - pip install flake8  # pytest  # add other testing frameworks later
before_script:
    - URL=https://github.com/${REPO}
    - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
    - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
    - cd ~/${REPO}
script:
    - echo stop the build if there are Python syntax errors or undefined names
    - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
    - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
    - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
    - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
    #- true  # pytest --capture=sys
notifications:
    on_success: change
    on_failure: change  # `always` will be the setting once code changes slow down
