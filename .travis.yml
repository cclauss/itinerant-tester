group: travis_latest
dist: xenial  # required for Python >= 3.7 (travis-ci/travis-ci#9069)
language: python
cache: pip
python:
  #- 2.7
  #- 3.5
  #- 3.6
  - 3.7
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
  - REPO=ccampbell/chromelogger-python
  - REPO=FreeCAD/FreeCAD
  - REPO=getredash/redash
  - REPO=joeyespo/pytest-watch
  - REPO=mitsuhiko/python-geoip
  - REPO=maxmind/GeoIP2-python  # has .travis.yml file
  - REPO=maxmind/HTTPretty  # has .travis.yml file
  - REPO=maxmind/MaxMind-DB-Reader-python  # has .travis.yml file
  - REPO=maxmind/minfraud-api-python  # has .travis.yml file
  - REPO=maxmind/pootle  # has .travis.yml file
  - REPO=druid-io/pydruid  # has .travis.yml file
  - REPO=ibmdb/python-ibmdb
  - REPO=dropbox/DropboxBusinessScripts
  - REPO=dropbox/PyHive  # has .travis.yml file
  - REPO=dropbox/changes-mesos-framework
  - REPO=dropbox/dropbox-api-v2-repl
  - REPO=dropbox/dropbox-sdk-python  # has .travis.yml file
  - REPO=dropbox/dropbox_hook
  - REPO=dropbox/git-rbr  # has .travis.yml file
  - REPO=dropbox/groupy  # has .travis.yml file
  - REPO=dropbox/hermes  # has .travis.yml file
  - REPO=dropbox/kafka-python  # has .travis.yml file
  - REPO=dropbox/llama-archive  # has .travis.yml file
  - REPO=dropbox/mdwebhook
  - REPO=dropbox/merou  # has .travis.yml file
  - REPO=dropbox/nautilus-dropbox
  - REPO=dropbox/nsot  # has .travis.yml file
  - REPO=dropbox/plyj  # has .travis.yml file
  - REPO=dropbox/pyannotate  # has .travis.yml file
  - REPO=dropbox/pygerduty  # has .travis.yml file
  - REPO=dropbox/pynsot  # has .travis.yml file
  - REPO=dropbox/pyston  # has .travis.yml file
  - REPO=dropbox/pyston-perf
  - REPO=dropbox/pytest-flakefinder  # has .travis.yml file
  - REPO=dropbox/pyxl
  - REPO=dropbox/rules_node
  - REPO=dropbox/securitybot  # has .travis.yml file
  - REPO=dropbox/sfmdemo
  - REPO=dropbox/sqlalchemy-stubs  # has .travis.yml file
  - REPO=dropbox/stone  # has .travis.yml file
  - REPO=dropbox/stopwatch  # has .travis.yml file
  - REPO=dropbox/trapperkeeper

install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=4.x
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E9,F63,F72,F82 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
