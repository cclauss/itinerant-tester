group: travis_latest
dist: xenial  # required for Python >= 3.7 (travis-ci/travis-ci#9069)
language: python
cache: pip
python:
  #- 2.7
  #- 3.4
  #- 3.5
  #- 3.6
  - 3.7
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
    - REPO=takluyver/astcheck  # has .travis.yml file
    - REPO=takluyver/astsearch  # has .travis.yml file
    - REPO=takluyver/backcall  # has .travis.yml file
    - REPO=takluyver/blog
    - REPO=takluyver/bookbook  # has .travis.yml file
    - REPO=takluyver/cfelpyutils
    - REPO=takluyver/edinburgh-dojo-2018-10
    - REPO=takluyver/entrypoints  # has .travis.yml file
    - REPO=takluyver/fabio  # has .travis.yml file
    - REPO=takluyver/flit  # has .travis.yml file
    - REPO=takluyver/h5py  # has .travis.yml file
    - REPO=takluyver/h5tools-py  # has .travis.yml file
    - REPO=takluyver/ipython  # has .travis.yml file
    - REPO=takluyver/jupyter_docker_kernels
    - REPO=takluyver/jupyter_kernel_mgmt  # has .travis.yml file
    - REPO=takluyver/jupyter_protocol  # has .travis.yml file
    - REPO=takluyver/jupyter_ssh_kernels
    - REPO=takluyver/nbopen
    - REPO=takluyver/nbparameterise  # has .travis.yml file
    - REPO=takluyver/penguin-carpentry
    - REPO=takluyver/peps  # has .travis.yml file
    - REPO=takluyver/pi_ina219  # has .travis.yml file
    - REPO=takluyver/pygame-flatpak-test
    - REPO=takluyver/pynsist  # has .travis.yml file
    - REPO=takluyver/pyxdg  # has .travis.yml file
    - REPO=takluyver/rednotebook  # has .travis.yml file
    - REPO=takluyver/sj
    - REPO=takluyver/snakeviz  # has .travis.yml file
    - REPO=takluyver/uk.python.org  # has .travis.yml file
    - REPO=takluyver/wheel2conda

install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
