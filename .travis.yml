group: travis_latest
dist: bionic
language: python
cache: pip
python:
  #- 2.7
  #- 3.5
  #- 3.6
  #- 3.7
  - 3.8
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
  - REPO=fighting41love/funNLP
  - REPO=google/clusterfuzz
  - REPO=google/clusterfuzz-tools
  - REPO=google/googletest
  - REPO=mherrmann/fbs -- Now Py3?
  - REPO=tensorflow/models
  - REPO=getsentry/sentry
  - REPO=nodejs/node
  - REPO=nodejs/node-gyp
  - REPO=Tribler/tribler
  - REPO=internetarchive/openlibrary
  - REPO=internetarchive/infogami
  - REPO=edx/edx-platform
  - REPO=edx/edx-e2e-tests
  - REPO=edx/testeng-ci
  - REPO=edx/notifier
  - REPO=edx/ecommerce-worker
  - REPO=edx/configuration
  - REPO=edx/ecommerce
  - REPO=edx/edx-analytics-data-api
  - REPO=edx/edx-analytics-pipeline
  - REPO=edx/edx-gomatic
  - REPO=edx/edx-notes-api
  - REPO=edx/edx-ora2
  - REPO=edx/edx-submissions
  - REPO=edx/edx-val
  - REPO=edx/edx-video-pipeline
  - REPO=edx/edx-video-worker
  - REPO=edx/xblock-lti-consumer
  - REPO=edx/xqueue

install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
