group: travis_latest
# dist: xenial  # required for Python 3.7
# sudo: true    # required for Python 3.7
language: python
env:
    - REPO=quantumlib/Cirq  # has .travis.yml file
    - REPO=quantumlib/OpenFermion  # has .travis.yml file
    - REPO=quantumlib/OpenFermion-Cirq  # has .travis.yml file
    - REPO=quantumlib/OpenFermion-ProjectQ  # has .travis.yml file
    - REPO=quantumlib/OpenFermion-Psi4
    - REPO=quantumlib/OpenFermion-PySCF  # has .travis.yml file
    - REPO=hugovk/Pillow  # has .travis.yml file
    - REPO=hugovk/aws-cli  # has .travis.yml file
    - REPO=hugovk/black  # has .travis.yml file
    - REPO=hugovk/botocore  # has .travis.yml file
    - REPO=hugovk/coveragepy  # has .travis.yml file
    - REPO=hugovk/dill  # has .travis.yml file
    - REPO=hugovk/drop-python  # has .travis.yml file
    - REPO=hugovk/fixtures  # has .travis.yml file
    - REPO=hugovk/flake8-bugbear  # has .travis.yml file
    - REPO=hugovk/frida-python
    - REPO=hugovk/github-tools  # has .travis.yml file
    - REPO=hugovk/gunicorn  # has .travis.yml file
    - REPO=hugovk/lastfm-tools  # has .travis.yml file
    - REPO=hugovk/lunchbot  # has .travis.yml file
    - REPO=hugovk/olefile  # has .travis.yml file
    - REPO=hugovk/openslide-python
    - REPO=hugovk/parso  # has .travis.yml file
    - REPO=hugovk/pika  # has .travis.yml file
    - REPO=hugovk/pixel-tools  # has .travis.yml file
    - REPO=hugovk/pyperclip
    - REPO=hugovk/pypi-tools  # has .travis.yml file
    - REPO=hugovk/pypinfo  # has .travis.yml file
    - REPO=hugovk/pytest  # has .travis.yml file
    - REPO=hugovk/pyyaml  # has .travis.yml file
    - REPO=hugovk/rq-dashboard  # has .travis.yml file
    - REPO=hugovk/scipy  # has .travis.yml file
    - REPO=hugovk/simplejson  # has .travis.yml file
    - REPO=hugovk/tinytext  # has .travis.yml file
    - REPO=hugovk/tldextract  # has .travis.yml file
    - REPO=hugovk/warehouse  # has .travis.yml file
 
cache: pip
python:
    #- 2.7
    - 3.6
    #- nightly
    #- pypy
    #- pypy3
matrix:
    allow_failures:
        - 2.7
        - python: nightly
        - python: pypy
        - python: pypy3
install:
    #- pip install -r requirements.txt
    - pip install flake8  # pytest  # add other testing frameworks later
before_script:
    - URL=https://github.com/${REPO}
    - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
    - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
    - cd ~/${REPO}
script:
    - echo stop the build if there are Python syntax errors or undefined names
    - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
    - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
    - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
    - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
    #- true  # pytest --capture=sys
notifications:
    on_success: change
    on_failure: change  # `always` will be the setting once code changes slow down
