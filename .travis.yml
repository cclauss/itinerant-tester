group: travis_latest
dist: xenial    # required for Python 3.7 (travis-ci/travis-ci#9069)
sudo: required  # required for Python 3.7 (travis-ci/travis-ci#9069)
language: python
cache: pip
python:
  #- 2.7
  #- 3.4
  #- 3.5
  #- 3.6
  - 3.7
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
  - REPO=s0md3v/XSStrike
  - REPO=santinic/pampy
  - REPO=openai/spinningup
  - REPO=google-research/bert
  - REPO=TheAlgorithms/Python
  - REPO=deeppomf/DeepCreamPy
  - REPO=tensorflow/models
  - REPO=chiphuyen/sotawhat
  - REPO=imhuay/Algorithm_Interview_Notes-Chinese
  - REPO=masa-su/pixyz
  - REPO=donnemartin/system-design-primer
  - REPO=vinta/awesome-python
  - REPO=allenai/writing-code-for-nlp-research-emnlp2018
  - REPO=NVIDIA/waveglow
  - REPO=MikeTheWatchGuy/PySimpleGUI
  - REPO=geekcomputers/Python
  - REPO=apachecn/awesome-algorithm
  - REPO=HTML-as-programming-language/HTML-as-programming-language
  - REPO=josephmisiti/awesome-machine-learning
  - REPO=rg3/youtube-dl
  - REPO=mirumee/saleor
  - REPO=liquanzhou/cedardeploy
  - REPO=keras-team/keras
  - REPO=django/django
  - REPO=facebookresearch/Horizon
  - REPO=ArduPilot/ardupilot
  - REPO=PythonCharmers/python-future
  - REPO=ansible/ansible
  - REPO=Tribler/tribler
  - REPO=vnpy/vnpy
  - REPO=oaubert/python-vlc
  - REPO=getsentry/sentry
  - REPO=CoreSecurity/impacket
  - REPO=matplotlib/matplotlib
  - REPO=ckan/ckan
  - REPO=ggtracker/sc2reader
  - REPO=apache/beam
  - REPO=apache/incubator-mxnet
  - REPO=apache/spark
  - REPO=ibm-watson-iot/functions
  - REPO=google/ffn
  - REPO=joxeankoret/pigaios
  - REPO=webpy/webpy
  - REPO=ibm-watson-iot/connector-cloudant
  - REPO=ibm-watson-iot/connector-qradar
  - REPO=ibm-watson-iot/connector-statsd
  - REPO=ibm-watson-iot/device-kodi
  - REPO=ibm-watson-iot/functions
  - REPO=ibm-watson-iot/gateway-hue
  - REPO=ibm-watson-iot/gateway-lightify
  - REPO=ibm-watson-iot/grafana4iot
  - REPO=ibm-watson-iot/iot-python
install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
