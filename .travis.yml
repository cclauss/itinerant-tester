group: travis_latest
dist: xenial  # required for Python >= 3.7 (travis-ci/travis-ci#9069)
language: python
cache: pip
python:
  #- 2.7
  #- 3.4
  #- 3.5
  #- 3.6
  - 3.7
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
  - REPO=google/GiftStick  # has .travis.yml file
  - REPO=google/aistreamer
  - REPO=google/capirca  # has .travis.yml file
  - REPO=google/ci_edit
  - REPO=google/citest
  - REPO=google/containerregistry  # has .travis.yml file
  - REPO=google/crmint  # has .travis.yml file
  - REPO=google/elemental2
  - REPO=google/flight-lab
  - REPO=google/ggrc-core
  - REPO=google/grr  # has .travis.yml file
  - REPO=google/grr-doc
  - REPO=google/jax  # has .travis.yml file
  - REPO=google/loaner  # has .travis.yml file
  - REPO=google/mirandum
  - REPO=google/myelin-acorn-electron-hardware
  - REPO=google/nucleus
  - REPO=google/openhtf  # has .travis.yml file
  - REPO=google/pasta  # has .travis.yml file
  - REPO=google/python-adb  # has .travis.yml file
  - REPO=google/python-spanner-orm
  - REPO=google/pytype  # has .travis.yml file
  - REPO=google/sanitizers
  - REPO=google/starlark-rust  # has .travis.yml file
  - REPO=google/swift-jupyter
  - REPO=google/tf_mesh_renderer
  - REPO=google/timesketch  # has .travis.yml file
  - REPO=google/turbinia  # has .travis.yml file
  - REPO=google/uis-rnn  # has .travis.yml file
  - REPO=google/yapf  # has .travis.yml file

install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
