group: travis_latest
dist: xenial  # required for Python >= 3.7 (travis-ci/travis-ci#9069)
language: python
cache: pip
python:
  #- 2.7
  #- 3.4
  #- 3.5
  #- 3.6
  - 3.7
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
    - REPO=hackebrot/PyPOM-screenshot
    - REPO=hackebrot/apistar  # has .travis.yml file
    - REPO=hackebrot/attrs  # has .travis.yml file
    - REPO=hackebrot/briefcase
    - REPO=hackebrot/changelogs  # has .travis.yml file
    - REPO=hackebrot/cookiecutter  # has .travis.yml file
    - REPO=hackebrot/cookiecutter-django  # has .travis.yml file
    - REPO=hackebrot/cookiecutter-examples
    - REPO=hackebrot/cookiedozer
    - REPO=hackebrot/cpython  # has .travis.yml file
    - REPO=hackebrot/django
    - REPO=hackebrot/django-rest-framework  # has .travis.yml file
    - REPO=hackebrot/dojo-asciiart
    - REPO=hackebrot/enhanced-coc-hotline
    - REPO=hackebrot/glean_parser  # has .travis.yml file
    - REPO=hackebrot/jinja2-time  # has .travis.yml file
    - REPO=hackebrot/labels  # has .travis.yml file
    - REPO=hackebrot/mozillians-tests  # has .travis.yml file
    - REPO=hackebrot/poyo  # has .travis.yml file
    - REPO=hackebrot/pytest  # has .travis.yml file
    - REPO=hackebrot/pytest-cookies  # has .travis.yml file
    - REPO=hackebrot/pytest-covfefe
    - REPO=hackebrot/pytest-tricks  # has .travis.yml file
    - REPO=hackebrot/python-bee
    - REPO=hackebrot/python-type-hints
    - REPO=hackebrot/redash
    - REPO=hackebrot/sfallhands
    - REPO=hackebrot/tox  # has .travis.yml file
    - REPO=hackebrot/uvloop  # has .travis.yml file
    - REPO=hackebrot/widget-cookiecutter
    
install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
