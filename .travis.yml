group: travis_latest
language: python
env:
    - REPO=GauravBh1010tt/DeepLearn
    - REPO=litl/backoff
    - REPO=python-security/pyt
    - REPO=tensorflow/models
    - REPO=hzy46/Deep-Learning-21-Examples
    - REPO=NVlabs/MUNIT
    - REPO=ustayready/CredKing
    - REPO=jhao104/proxy_pool
    - REPO=chiphuyen/stanford-tensorflow-tutorials
    - REPO=keras-team/keras
    - REPO=rg3/youtube-dl
    - REPO=vinta/awesome-python
    - REPO=roytseng-tw/mask-rcnn.pytorch
    - REPO=llSourcell/Make_Money_with_Tensorflow
    - REPO=levyben/DeepSuperLearner
    - REPO=a2u/CVE-2018-7600
    - REPO=django/django
    - REPO=jonas-koehler/s2cnn
    - REPO=pallets/flask
    - REPO=Ahmkel/Keras-Project-Template
    - REPO=scikit-learn/scikit-learn
    - REPO=CSAILVision/semantic-segmentation-pytorch
    - REPO=hannob/snallygaster
    - REPO=matterport/Mask_RCNN
    - REPO=josephmisiti/awesome-machine-learning
    - REPO=matplotlib/matplotlib
    - REPO=PythonCharmers/python-future
    - REPO=ansible/ansible
    - REPO=ckan/ckan
    - REPO=facebookresearch/ParlAI
    - REPO=gevent/gevent
    - REPO=getsentry/sentry
    - REPO=ggtracker/sc2reader
    - REPO=apache/beam
    - REPO=apache/spark
    - REPO=hyperledger/fabric
cache: pip
python:
    #- 2.7
    - 3.6
    #- nightly
    #- pypy
    #- pypy3
matrix:
    allow_failures:
        - python: nightly
        - python: pypy
        - python: pypy3
install:
    #- pip install -r requirements.txt
    - pip install flake8  # pytest  # add other testing frameworks later
before_script:
    - URL=https://github.com/${REPO}
    - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
    - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
    - cd ~/${REPO}
script:
    - echo stop the build if there are Python syntax errors or undefined names
    - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
    - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
    - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
    - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
    #- true  # pytest --capture=sys
notifications:
    on_success: change
    on_failure: change  # `always` will be the setting once code changes slow down
