group: travis_latest
dist: xenial    # required for Python 3.7 (travis-ci/travis-ci#9069)
sudo: required  # required for Python 3.7 (travis-ci/travis-ci#9069)
language: python
cache: pip
python:
  #- 2.7
  #- 3.4
  #- 3.5
  #- 3.6
  - 3.7
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
  - REPO=deepmind/graph_nets
  - REPO=open-source-for-science/TensorFlow-Course
  - REPO=TheAlgorithms/Python
  - REPO=deepmind/trfl
  - REPO=codertimo/BERT-pytorch
  - REPO=socialcopsdev/camelot
  - REPO=Tencent/Metis
  - REPO=tensorflow/models
  - REPO=donnemartin/system-design-primer
  - REPO=vinta/awesome-python
  - REPO=tugstugi/pytorch-saltnet
  - REPO=open-mmlab/mmdetection
  - REPO=rg3/youtube-dl
  - REPO=azavea/raster-vision
  - REPO=home-assistant/home-assistant
  - REPO=imhuay/Algorithm_Interview_Notes-Chinese
  - REPO=hackerearthclub/CODE2RACE
  - REPO=malwaredllc/byob
  - REPO=django/django
  - REPO=gbafana25/esp8266_honeypot
  - REPO=Th3J0kr/jtb_investigator
  - REPO=toddmotto/public-apis
  - REPO=internetarchive/openlibrary
  - REPO=python/cpython
  - REPO=pallets/flask
  - REPO=ArduPilot/ardupilot
  - REPO=PythonCharmers/python-future
  - REPO=ansible/ansible
  - REPO=Tribler/tribler
  - REPO=vnpy/vnpy
  - REPO=oaubert/python-vlc
  - REPO=getsentry/sentry
  - REPO=CoreSecurity/impacket
  - REPO=matplotlib/matplotlib
  - REPO=ckan/ckan
  - REPO=ggtracker/sc2reader
  - REPO=apache/beam
  - REPO=apache/incubator-mxnet
  - REPO=apache/spark
install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
