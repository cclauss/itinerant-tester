group: travis_latest
language: python
env:
    - REPO=pypa/auditwheel  # has .travis.yml file
    - REPO=pypa/browntruck
    - REPO=pypa/conveyor
    - REPO=pypa/forklift
    - REPO=pypa/get-pip
    - REPO=pypa/interoperability-peps
    - REPO=pypa/linehaul  # has .travis.yml file
    - REPO=pypa/manylinux  # has .travis.yml file
    - REPO=pypa/packaging  # has .travis.yml file
    - REPO=pypa/pep470
    - REPO=pypa/pep517  # has .travis.yml file
    - REPO=pypa/pip  # has .travis.yml file
    - REPO=pypa/pipenv  # has .travis.yml file
    - REPO=pypa/pipfile  # has .travis.yml file
    - REPO=pypa/pkg_resources  # has .travis.yml file
    - REPO=pypa/pypa-docs-theme
    - REPO=pypa/pypa.io
    - REPO=pypa/pypi-legacy
    - REPO=pypa/python-packaging-user-guide  # has .travis.yml file
    - REPO=pypa/readme_renderer  # has .travis.yml file
    - REPO=pypa/sample-namespace-packages
    - REPO=pypa/sampleproject  # has .travis.yml file
    - REPO=pypa/scripttest  # has .travis.yml file
    - REPO=pypa/setuptools  # has .travis.yml file
    - REPO=pypa/setuptools_scm  # has .travis.yml file
    - REPO=pypa/ssl  # has .travis.yml file
    - REPO=pypa/twine  # has .travis.yml file
    - REPO=pypa/virtualenv  # has .travis.yml file
    - REPO=pypa/warehouse  # has .travis.yml file
    - REPO=pypa/wheel  # has .travis.yml filecache: pip
python:
    #- 2.7
    - 3.6
    #- nightly
    #- pypy
    #- pypy3
matrix:
    allow_failures:
        - python: nightly
        - python: pypy
        - python: pypy3
install:
    #- pip install -r requirements.txt
    - pip install flake8  # pytest  # add other testing frameworks later
before_script:
    - URL=https://github.com/${REPO}
    - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
    - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
    - cd ~/${REPO}
script:
    - echo stop the build if there are Python syntax errors or undefined names
    - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
    - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
    - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
    - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
    #- true  # pytest --capture=sys
notifications:
    on_success: change
    on_failure: change  # `always` will be the setting once code changes slow down
