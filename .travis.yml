group: travis_latest
dist: xenial  # required for Python >= 3.7 (travis-ci/travis-ci#9069)
language: python
cache: pip
python:
  #- 2.7
  #- 3.4
  #- 3.5
  #- 3.6
  - 3.7
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
    - REPO=rciorba/1984
    - REPO=rciorba/andreeafarago-plp
    - REPO=rciorba/bitbucket_pr
    - REPO=rciorba/borg  # has .travis.yml file
    - REPO=rciorba/cookiecutter-pytest-plugin  # has .travis.yml file
    - REPO=rciorba/curs-python
    - REPO=rciorba/ddns
    - REPO=rciorba/django-haystack  # has .travis.yml file
    - REPO=rciorba/elasticsearch-py  # has .travis.yml file
    - REPO=rciorba/elasticsearch-py-async
    - REPO=rciorba/es_mon
    - REPO=rciorba/etcd_membership_plugin  # has .travis.yml file
    - REPO=rciorba/glass_2_kibana
    - REPO=rciorba/hashcode_2017
    - REPO=rciorba/iconcur
    - REPO=rciorba/jam
    - REPO=rciorba/moin-2.0-mirror
    - REPO=rciorba/pgzero-flappy
    - REPO=rciorba/pidunu  # has .travis.yml file
    - REPO=rciorba/plp-cosmin
    - REPO=rciorba/pytest  # has .travis.yml file
    - REPO=rciorba/pytest-django  # has .travis.yml file
    - REPO=rciorba/pytest-fixture-marker  # has .travis.yml file
    - REPO=rciorba/rotten
    - REPO=rciorba/thespian  # has .travis.yml file
    - REPO=rciorba/tldt
    - REPO=rciorba/twit
    - REPO=rciorba/virtosubogdan-plp
    - REPO=rciorba/vlddamian-plp
    - REPO=rciorba/z3  # has .travis.yml file

install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
