group: travis_latest
# dist: xenial  # required for Python 3.7
# sudo: true    # required for Python 3.7
language: python
env:
    - REPO=nbedos/termtosvg
    - REPO=rg3/youtube-dl
    - REPO=danmacnish/cartoonify
    - REPO=x0rz/tweets_analyzer
    - REPO=tensorflow/models
    - REPO=ThoughtfulDev/EagleEye
    - REPO=mps-youtube/mps-youtube
    - REPO=OlafenwaMoses/ImageAI
    - REPO=AdeelMufti/WorldModels
    - REPO=keras-team/keras
    - REPO=vinta/awesome-python
    - REPO=DeuroIO/erc20-ico-onchain-technical-analysis
    - REPO=chubin/cheat.sh
    - REPO=vibora-io/vibora
    - REPO=Aidaho12/haproxy-wi
    - REPO=toddmotto/public-apis
    - REPO=requests/requests
    - REPO=databricks/mlflow
    - REPO=pallets/flask
    - REPO=AlexiaJM/RelativisticGAN
    - REPO=django/django
    - REPO=ansible/ansible
    - REPO=donnemartin/system-design-primer
    - REPO=pypa/pipenv
    - REPO=XX-net/XX-Net
    - REPO=matplotlib/matplotlib
    - REPO=ckan/ckan
    - REPO=ggtracker/sc2reader
    - REPO=hyperledger/fabric
    - REPO=vnpy/vnpy
    - REPO=PythonCharmers/python-future
    - REPO=ansible/ansible
    - REPO=Tribler/tribler
    - REPO=apache/beam
    - REPO=apache/spark
    - REPO=oaubert/python-vlc
    - REPO=gevent/gevent
    - REPO=getsentry/sentry
    - REPO=mrJean1/PyGeodesy
cache: pip
python:
    #- 2.7
    - 3.6
    #- nightly
    #- pypy
    #- pypy3
matrix:
    allow_failures:
        - 2.7
        - python: nightly
        - python: pypy
        - python: pypy3
install:
    #- pip install -r requirements.txt
    - pip install flake8  # pytest  # add other testing frameworks later
before_script:
    - URL=https://github.com/${REPO}
    - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
    - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
    - cd ~/${REPO}
script:
    - echo stop the build if there are Python syntax errors or undefined names
    - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
    - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
    - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
    - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
    #- true  # pytest --capture=sys
notifications:
    on_success: change
    on_failure: change  # `always` will be the setting once code changes slow down
