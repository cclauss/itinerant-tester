group: travis_latest
language: python
env:
    - REPO=facebookresearch/BlueWhale
    - REPO=facebookresearch/CommAI-env
    - REPO=facebookresearch/DetectAndTrack
    - REPO=facebookresearch/Detectron
    - REPO=facebookresearch/DrQA
    - REPO=facebookresearch/FaderNetworks
    - REPO=facebookresearch/GradientEpisodicMemory
    - REPO=facebookresearch/InferSent
    - REPO=facebookresearch/MLDoc
    - REPO=facebookresearch/MUSE
    - REPO=facebookresearch/ParlAI
    - REPO=facebookresearch/R2Plus1D
    - REPO=facebookresearch/SentEval
    - REPO=facebookresearch/adversarial_image_defenses
    - REPO=facebookresearch/clevr-dataset-gen
    - REPO=facebookresearch/clevr-iep
    - REPO=facebookresearch/colorlessgreenRNNs
    - REPO=facebookresearch/end-to-end-negotiator
    - REPO=facebookresearch/loop
    - REPO=facebookresearch/low-shot-shrink-hallucinate
    - REPO=facebookresearch/mixup-cifar10
    - REPO=facebookresearch/odin
    - REPO=facebookresearch/poincare-embeddings
    - REPO=facebookresearch/py2bpf
    - REPO=facebookresearch/pysparnn
    - REPO=facebookresearch/supervised-hashing-baselines
    - REPO=facebookresearch/tdfbanks
    - REPO=facebookresearch/video-nonlocal-net
    - REPO=facebookresearch/visdom
cache: pip
python:
    #- 2.7
    - 3.6
    #- nightly
    #- pypy
    #- pypy3
matrix:
    allow_failures:
        - python: nightly
        - python: pypy
        - python: pypy3
install:
    #- pip install -r requirements.txt
    - pip install flake8  # pytest  # add other testing frameworks later
before_script:
    - URL=https://github.com/${REPO}
    - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
    - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
    - cd ~/${REPO}
script:
    - echo stop the build if there are Python syntax errors or undefined names
    - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
    - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
    - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
    - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
    #- true  # pytest --capture=sys
notifications:
    on_success: change
    on_failure: change  # `always` will be the setting once code changes slow down
