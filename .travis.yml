group: travis_latest
dist: xenial  # required for Python >= 3.7 (travis-ci/travis-ci#9069)
language: python
cache: pip
python:
  #- 2.7
  #- 3.4
  #- 3.5
  #- 3.6
  - 3.7
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
    - REPO=Mariatta/aiohttp-session  # has .travis.yml file
    - REPO=Mariatta/aiojobs  # has .travis.yml file
    - REPO=Mariatta/arq  # has .travis.yml file
    - REPO=Mariatta/async-server
    - REPO=Mariatta/bedevere  # has .travis.yml file
    - REPO=Mariatta/black  # has .travis.yml file
    - REPO=Mariatta/black_out
    - REPO=Mariatta/blurb_it  # has .travis.yml file
    - REPO=Mariatta/celery  # has .travis.yml file
    - REPO=Mariatta/check_python_cla  # has .travis.yml file
    - REPO=Mariatta/coveragepy  # has .travis.yml file
    - REPO=Mariatta/cpython  # has .travis.yml file
    - REPO=Mariatta/devguide  # has .travis.yml file
    - REPO=Mariatta/enhanced-coc-hotline  # has .travis.yml file
    - REPO=Mariatta/flit  # has .travis.yml file
    - REPO=Mariatta/gh_app_demo
    - REPO=Mariatta/gidgethub  # has .travis.yml file
    - REPO=Mariatta/gidgethub-aiohttp-starter
    - REPO=Mariatta/github-bot-tutorial  # has .travis.yml file
    - REPO=Mariatta/miss-islington  # has .travis.yml file
    - REPO=Mariatta/nexmo-python  # has .travis.yml file
    - REPO=Mariatta/peps  # has .travis.yml file
    - REPO=Mariatta/pylint  # has .travis.yml file
    - REPO=Mariatta/pytaco
    - REPO=Mariatta/python_versions_and_distros  # has .travis.yml file
    - REPO=Mariatta/strange-relationship
    - REPO=Mariatta/talk-talk-talk  # has .travis.yml file
    - REPO=Mariatta/tic_tac_taco_pizza
    - REPO=Mariatta/vibora
    - REPO=Mariatta/welcome-wagon-2018  # has .travis.yml file

install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
