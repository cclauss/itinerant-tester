group: travis_latest
os: linux
dist: focal
language: python
cache: pip
python:
  #- 2.7
  #- 3.5
  #- 3.6
  #- 3.7
  #- 3.8
  - 3.9-dev
jobs:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
  - REPO=sberbank-ai/ru-gpts
  - REPO=emeryberger/scalene
  - REPO=PyTorchLightning/pytorch-lightning
  - REPO=ansible/ansible
  - REPO=satwikkansal/wtfpython
  - REPO=rwightman/pytorch-image-models
  - REPO=bridgecrewio/checkov
  - REPO=Gr1mmie/Practical-Ethical-Hacking-Resources
  - REPO=TensorSpeech/TensorFlowTTS
  - REPO=swisskyrepo/PayloadsAllTheThings
  - REPO=adamerose/pandasgui
  - REPO=Azure/azure-sdk-for-python
  - REPO=TobiasPankner/Teams-Auto-Joiner
  - REPO=dataframehq/whale
  - REPO=sherlock-project/sherlock
  - REPO=zulip/zulip
  - REPO=tiangolo/fastapi
  - REPO=fishtown-analytics/dbt
  - REPO=NVIDIA/DeepLearningExamples
  - REPO=yzhao062/anomaly-detection-resources
  - REPO=microsoft/unilm
  - REPO=dreamnettech/dreampower
  - REPO=n1nj4sec/pupy
  - REPO=PythonCharmers/python-future
  - REPO=ansible/ansible
  - REPO=google/oss-fuzz
  - REPO=GoogleCloudPlatform/professional-services
  - REPO=oaubert/python-vlc
  - REPO=ColdGrub1384/Pyto
  - REPO=internetarchive/openlibrary
  - REPO=apache/beam
  - REPO=apache/incubator-mxnet
  - REPO=hyperledger/iroha-python
  - REPO=Supervisor/supervisor
  - REPO=cheshirekow/cmake_format
  - REPO=ckan/ckan
  - REPO=ibm-watson-iot/connector-cloudant
  - REPO=ibm-watson-iot/device-kodi
  - REPO=ibm-watson-iot/functions
  - REPO=hyperledger/fabric-sdk-py
  - REPO=v8/v8
  - REPO=webpy/webpy
  - REPO=nodejs/node
  - REPO=nodejs/node-gyp
  - REPO=ArduPilot/ardupilot
  - REPO=ArduPilot/pymavlink
  - REPO=dronekit/dronekit-python
  - REPO=ansible/awx
  - REPO=matplotlib/matplotlib
  - REPO=ggtracker/sc2reader
  - REPO=httplib2/httplib2
  - REPO=getsentry/sentry
  - REPO=getsentry/sentry-python
  - REPO=CoreSecurity/impacket
  - REPO=google/ffn
  - REPO=wxWidgets/Phoenix
  - REPO=pyinstaller/pyinstaller
  - REPO=hyperopt/hyperopt
install:
  #- pip install -r requirements.txt
  - pip install codespell flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  - codespell . --ignore-words-list=ba,referer --skip=./.* --quiet-level=2 || true
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
