group: travis_latest
dist: xenial    # required for Python 3.7 (travis-ci/travis-ci#9069)
sudo: required  # required for Python 3.7 (travis-ci/travis-ci#9069)
language: python
cache: pip
python:
  #- 2.7
  #- 3.4
  #- 3.5
  #- 3.6
  - 3.7
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
    - REPO=internetarchive/IA-solr-frontend
    - REPO=internetarchive/IAWatchBot
    - REPO=internetarchive/acs4_py
    - REPO=internetarchive/analyze_ocr
    - REPO=internetarchive/bookserver
    - REPO=internetarchive/brozzler  # has .travis.yml file
    - REPO=internetarchive/btget
    - REPO=internetarchive/crawling-for-nomore404
    - REPO=internetarchive/doublethink  # has .travis.yml file
    - REPO=internetarchive/draintasker
    - REPO=internetarchive/dweb-gateway  # has .travis.yml file
    - REPO=internetarchive/epub
    - REPO=internetarchive/epub3
    - REPO=internetarchive/infogami
    - REPO=internetarchive/liveweb  # has .travis.yml file
    - REPO=internetarchive/map-of-the-web
    - REPO=internetarchive/openlibrary  # has .travis.yml file
    - REPO=internetarchive/openlibrary-bots
    - REPO=internetarchive/openlibrary-client  # has .travis.yml file
    - REPO=internetarchive/py-wasapi-client  # has .travis.yml file
    - REPO=internetarchive/s3_loader
    - REPO=internetarchive/scribe-redux
    - REPO=internetarchive/surt  # has .travis.yml file
    - REPO=internetarchive/trough  # has .travis.yml file
    - REPO=internetarchive/umbra
    - REPO=internetarchive/waimea
    - REPO=internetarchive/warc  # has .travis.yml file
    - REPO=internetarchive/warcprox  # has .travis.yml file
    - REPO=internetarchive/warctools  # has .travis.yml file
    - REPO=internetarchive/wayback-discover-diff
    - REPO=cdrini/openlibrary-wikidata-bot
    - REPO=hornc/ixqus
    - REPO=hornc/marcia
    - REPO=hornc/catharbot
    - REPO=hornc/ol-schemata
    - REPO=niolabs/python-xbee

install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
