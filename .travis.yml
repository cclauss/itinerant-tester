group: travis_latest
language: python
env:
    - REPO=mapbox/COGDumper  # has .travis.yml file
    - REPO=mapbox/CTFd  # has .travis.yml file
    - REPO=mapbox/fio-area
    - REPO=mapbox/make-surface
    - REPO=mapbox/mapbox-cli-py  # has .travis.yml file
    - REPO=mapbox/mapbox-sdk-py  # has .travis.yml file
    - REPO=mapbox/mapboxgl-jupyter  # has .travis.yml file
    - REPO=mapbox/mbutil  # has .travis.yml file
    - REPO=mapbox/mercantile  # has .travis.yml file
    - REPO=mapbox/montilecarlo
    - REPO=mapbox/nodata
    - REPO=mapbox/osmcha  # has .travis.yml file
    - REPO=mapbox/pxm-manifest-specification
    - REPO=mapbox/raster-tester
    - REPO=mapbox/rasterio  # has .travis.yml file
    - REPO=mapbox/rasterio-cookbook
    - REPO=mapbox/rio-alpha  # has .travis.yml file
    - REPO=mapbox/rio-color  # has .travis.yml file
    - REPO=mapbox/rio-mbtiles  # has .travis.yml file
    - REPO=mapbox/rio-merge-rgba  # has .travis.yml file
    - REPO=mapbox/rio-polyencode
    - REPO=mapbox/rio-rgbify  # has .travis.yml file
    - REPO=mapbox/rio-tiler
    - REPO=mapbox/robosat  # has .travis.yml file
    - REPO=mapbox/snuggs  # has .travis.yml file
    - REPO=mapbox/supermercado  # has .travis.yml file
    - REPO=mapbox/untiler  # has .travis.yml file
    - REPO=mapbox/vector-tile-base
    - REPO=mapbox/vector-tile-py
    - REPO=mapbox/watchbot-progress-py  # has .travis.yml file    - REPO=h2oai/typesentry

cache: pip
python:
    #- 2.7
    - 3.6
    #- nightly
    #- pypy
    #- pypy3
matrix:
    allow_failures:
        - python: 2.7
        - python: nightly
        - python: pypy
        - python: pypy3
install:
    #- pip install -r requirements.txt
    - pip install flake8  # pytest  # add other testing frameworks later
before_script:
    - URL=https://github.com/${REPO}
    - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
    - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
    - cd ~/${REPO}
script:
    - echo stop the build if there are Python syntax errors or undefined names
    - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
    - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
    - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
    - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
    #- true  # pytest --capture=sys
notifications:
    on_success: change
    on_failure: change  # `always` will be the setting once code changes slow down
