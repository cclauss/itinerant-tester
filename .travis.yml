group: travis_latest
language: python
env:
    - REPO=jeffzh3ng/Fuxi-Scanner
    - REPO=jeffzh3ng/InsectsAwake
    - REPO=jeffzh3ng/KIT
    - REPO=nedbat/adventofcode2015
    - REPO=nedbat/adventofcode2016
    - REPO=nedbat/adventofcode2017
    - REPO=nedbat/attrs  # has .travis.yml file
    - REPO=nedbat/bitbucket-issue-migration
    - REPO=nedbat/blogtools
    - REPO=nedbat/blowyournose
    - REPO=nedbat/byterun
    - REPO=nedbat/cookiecutter-djangopackage  # has .travis.yml file
    - REPO=nedbat/coveragepy  # has .travis.yml file
    - REPO=nedbat/coveragepy-bbmirror  # has .travis.yml file
    - REPO=nedbat/cpython  # has .travis.yml file
    - REPO=nedbat/django_coverage_plugin  # has .travis.yml file
    - REPO=nedbat/django_issue_25793
    - REPO=nedbat/doc8
    - REPO=nedbat/help-tokens
    - REPO=nedbat/isect_segments-bentley_ottmann
    - REPO=nedbat/natsworld
    - REPO=nedbat/pydoctor
    - REPO=nedbat/pyhurry
    - REPO=nedbat/pylint  # has .travis.yml file
    - REPO=nedbat/pylintdb
    - REPO=nedbat/pytest  # has .travis.yml file
    - REPO=nedbat/python-aspectlib  # has .travis.yml file
    - REPO=nedbat/requests  # has .travis.yml file
    - REPO=nedbat/rst2ctags
    - REPO=nedbat/tabtest
    - REPO=nedbat/unittest-mixins
    - REPO=nedbat/wikicrawl
    - REPO=nedbat/zellij

cache: pip
python:
    #- 2.7
    - 3.6
    #- nightly
    #- pypy
    #- pypy3
matrix:
    allow_failures:
        - python: nightly
        - python: pypy
        - python: pypy3
install:
    #- pip install -r requirements.txt
    - pip install flake8  # pytest  # add other testing frameworks later
before_script:
    - URL=https://github.com/${REPO}
    - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
    - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
    - cd ~/${REPO}
script:
    - echo stop the build if there are Python syntax errors or undefined names
    - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
    - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
    - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
    - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
    #- true  # pytest --capture=sys
notifications:
    on_success: change
    on_failure: change  # `always` will be the setting once code changes slow down
