group: travis_latest
dist: xenial  # required for Python >= 3.7 (travis-ci/travis-ci#9069)
language: python
cache: pip
python:
  #- 2.7
  #- 3.4
  #- 3.5
  #- 3.6
  - 3.7
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
  - REPO=coryleeio/Telephone
  - REPO=coryleeio/django-seed
  - REPO=coryleeio/simple-docker-demo
(PyPI_Top_5000_Notifier) ccc-macbook-pro-2:trending_tester cclauss$ ./re* singuliere
https://github.com/singuliere?tab=repositories&language=python
  - REPO=singuliere/ansible-ansible_plugins  # has .travis.yml file
  - REPO=singuliere/codejail
  - REPO=singuliere/django-pyfs  # has .travis.yml file
  - REPO=singuliere/edx-platform
  - REPO=singuliere/edx-val  # has .travis.yml file
  - REPO=singuliere/enough
  - REPO=singuliere/event-tracking  # has .travis.yml file
  - REPO=singuliere/hvac  # has .travis.yml file
  - REPO=singuliere/molecule  # has .travis.yml file
  - REPO=singuliere/open-edx-proposals
  - REPO=singuliere/pykeepass  # has .travis.yml file
  - REPO=singuliere/pylint-celery  # has .travis.yml file
  - REPO=singuliere/python-icinga2api
  - REPO=singuliere/securedrop  # has .travis.yml file
  - REPO=singuliere/testinfra  # has .travis.yml file
  - REPO=singuliere/vault-cli  # has .travis.yml file
  - REPO=singuliere/vault-keepass-import
  - REPO=singuliere/web-fragments  # has .travis.yml file
install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
