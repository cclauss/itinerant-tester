sudo: false
dist: trusty
language: python
python:
    - 2.7
    - 3.6.1
#before_install:
    #- REPO_TO_TEST=https://github.com/ibm-watson-data-lab/ibmseti
    #- REPO_TO_TEST=https://github.com/AlexiaJM/Deep-learning-with-cats
    #- REPO_TO_TEST=https://github.com/vividvilla/csvtotable
    #- REPO_TO_TEST=https://github.com/cloudant/python-cloudant
    #- git clone --depth=50 --branch=master ${REPO_TO_TEST} ~/itinerant-tester
    ##- cp *.py ~/itinerant-tester  # copy local python scripts into ~/itinerant-tester
    #- cd ~/itinerant-tester
install:
    - pip install flake8  # pytest  # add some test later
before_script:
    # 1 flake8 testing of: probcomp/bayeslite...
    - git clone --depth=50 --branch=master probcomp/bayeslite ~/bayeslite
    - cd ~/bayeslite
    # stop the build if there are Python syntax errors or undefined names
    - flake8 . --count --select=E901,E999,F821,F822,F823 --statistics
    # exit-zero treates all errors as warnings.  The GitHub editor is 127 chars wide
    - flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    # 2 flake8 testing of: AlexiaJM/Deep-learning-with-cats...
    - git clone --depth=50 --branch=master AlexiaJM/Deep-learning-with-cats ~/Deep-learning-with-cats
    - cd ~/Deep-learning-with-cats
    # stop the build if there are Python syntax errors or undefined names
    - flake8 . --count --select=E901,E999,F821,F822,F823 --statistics
    # exit-zero treates all errors as warnings.  The GitHub editor is 127 chars wide
    - flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    # 3 flake8 testing of: iogf/crocs...
    - git clone --depth=50 --branch=master iogf/crocs ~/crocs
    - cd ~/crocs
    # stop the build if there are Python syntax errors or undefined names
    - flake8 . --count --select=E901,E999,F821,F822,F823 --statistics
    # exit-zero treates all errors as warnings.  The GitHub editor is 127 chars wide
    - flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    # 4 flake8 testing of: 0x4D31/honeyLambda...
    - git clone --depth=50 --branch=master 0x4D31/honeyLambda ~/honeyLambda
    - cd ~/honeyLambda
    # stop the build if there are Python syntax errors or undefined names
    - flake8 . --count --select=E901,E999,F821,F822,F823 --statistics
    # exit-zero treates all errors as warnings.  The GitHub editor is 127 chars wide
    - flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    # 5 flake8 testing of: tensorflow/models...
    - git clone --depth=50 --branch=master tensorflow/models ~/models
    - cd ~/models
    # stop the build if there are Python syntax errors or undefined names
    - flake8 . --count --select=E901,E999,F821,F822,F823 --statistics
    # exit-zero treates all errors as warnings.  The GitHub editor is 127 chars wide
    - flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    # 6 flake8 testing of: biancasubion/jupyshare...
    - git clone --depth=50 --branch=master biancasubion/jupyshare ~/jupyshare
    - cd ~/jupyshare
    # stop the build if there are Python syntax errors or undefined names
    - flake8 . --count --select=E901,E999,F821,F822,F823 --statistics
    # exit-zero treates all errors as warnings.  The GitHub editor is 127 chars wide
    - flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    # 7 flake8 testing of: jadore801120/attention-is-all-you-need-pytorch...
    - git clone --depth=50 --branch=master jadore801120/attention-is-all-you-need-pytorch ~/attention-is-all-you-need-pytorch
    - cd ~/attention-is-all-you-need-pytorch
    # stop the build if there are Python syntax errors or undefined names
    - flake8 . --count --select=E901,E999,F821,F822,F823 --statistics
    # exit-zero treates all errors as warnings.  The GitHub editor is 127 chars wide
    - flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    # 8 flake8 testing of: python/cpython...
    - git clone --depth=50 --branch=master python/cpython ~/cpython
    - cd ~/cpython
    # stop the build if there are Python syntax errors or undefined names
    - flake8 . --count --select=E901,E999,F821,F822,F823 --statistics
    # exit-zero treates all errors as warnings.  The GitHub editor is 127 chars wide
    - flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    # 9 flake8 testing of: warner/magic-wormhole...
    - git clone --depth=50 --branch=master warner/magic-wormhole ~/magic-wormhole
    - cd ~/magic-wormhole
    # stop the build if there are Python syntax errors or undefined names
    - flake8 . --count --select=E901,E999,F821,F822,F823 --statistics
    # exit-zero treates all errors as warnings.  The GitHub editor is 127 chars wide
    - flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    # 10 flake8 testing of: rg3/youtube-dl...
    - git clone --depth=50 --branch=master rg3/youtube-dl ~/youtube-dl
    - cd ~/youtube-dl
    # stop the build if there are Python syntax errors or undefined names
    - flake8 . --count --select=E901,E999,F821,F822,F823 --statistics
    # exit-zero treates all errors as warnings.  The GitHub editor is 127 chars wide
    - flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    # 11 flake8 testing of: jordanpotti/AWSBucketDump...
    - git clone --depth=50 --branch=master jordanpotti/AWSBucketDump ~/AWSBucketDump
    - cd ~/AWSBucketDump
    # stop the build if there are Python syntax errors or undefined names
    - flake8 . --count --select=E901,E999,F821,F822,F823 --statistics
    # exit-zero treates all errors as warnings.  The GitHub editor is 127 chars wide
    - flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    # 12 flake8 testing of: vinta/awesome-python...
    - git clone --depth=50 --branch=master vinta/awesome-python ~/awesome-python
    - cd ~/awesome-python
    # stop the build if there are Python syntax errors or undefined names
    - flake8 . --count --select=E901,E999,F821,F822,F823 --statistics
    # exit-zero treates all errors as warnings.  The GitHub editor is 127 chars wide
    - flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    # 13 flake8 testing of: vividvilla/csvtotable...
    - git clone --depth=50 --branch=master vividvilla/csvtotable ~/csvtotable
    - cd ~/csvtotable
    # stop the build if there are Python syntax errors or undefined names
    - flake8 . --count --select=E901,E999,F821,F822,F823 --statistics
    # exit-zero treates all errors as warnings.  The GitHub editor is 127 chars wide
    - flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    # 14 flake8 testing of: Arvanaghi/CheckPlease...
    - git clone --depth=50 --branch=master Arvanaghi/CheckPlease ~/CheckPlease
    - cd ~/CheckPlease
    # stop the build if there are Python syntax errors or undefined names
    - flake8 . --count --select=E901,E999,F821,F822,F823 --statistics
    # exit-zero treates all errors as warnings.  The GitHub editor is 127 chars wide
    - flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    # 15 flake8 testing of: scikit-learn/scikit-learn...
    - git clone --depth=50 --branch=master scikit-learn/scikit-learn ~/scikit-learn
    - cd ~/scikit-learn
    # stop the build if there are Python syntax errors or undefined names
    - flake8 . --count --select=E901,E999,F821,F822,F823 --statistics
    # exit-zero treates all errors as warnings.  The GitHub editor is 127 chars wide
    - flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    # 16 flake8 testing of: andreiapostoae/dota2-predictor...
    - git clone --depth=50 --branch=master andreiapostoae/dota2-predictor ~/dota2-predictor
    - cd ~/dota2-predictor
    # stop the build if there are Python syntax errors or undefined names
    - flake8 . --count --select=E901,E999,F821,F822,F823 --statistics
    # exit-zero treates all errors as warnings.  The GitHub editor is 127 chars wide
    - flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    # 17 flake8 testing of: ansible/ansible...
    - git clone --depth=50 --branch=master ansible/ansible ~/ansible
    - cd ~/ansible
    # stop the build if there are Python syntax errors or undefined names
    - flake8 . --count --select=E901,E999,F821,F822,F823 --statistics
    # exit-zero treates all errors as warnings.  The GitHub editor is 127 chars wide
    - flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    # 18 flake8 testing of: stanford-futuredata/noscope...
    - git clone --depth=50 --branch=master stanford-futuredata/noscope ~/noscope
    - cd ~/noscope
    # stop the build if there are Python syntax errors or undefined names
    - flake8 . --count --select=E901,E999,F821,F822,F823 --statistics
    # exit-zero treates all errors as warnings.  The GitHub editor is 127 chars wide
    - flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    # 19 flake8 testing of: jisungk/RIDDLE...
    - git clone --depth=50 --branch=master jisungk/RIDDLE ~/RIDDLE
    - cd ~/RIDDLE
    # stop the build if there are Python syntax errors or undefined names
    - flake8 . --count --select=E901,E999,F821,F822,F823 --statistics
    # exit-zero treates all errors as warnings.  The GitHub editor is 127 chars wide
    - flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    # 20 flake8 testing of: Eyyub/tensorflow-pix2pix...
    - git clone --depth=50 --branch=master Eyyub/tensorflow-pix2pix ~/tensorflow-pix2pix
    - cd ~/tensorflow-pix2pix
    # stop the build if there are Python syntax errors or undefined names
    - flake8 . --count --select=E901,E999,F821,F822,F823 --statistics
    # exit-zero treates all errors as warnings.  The GitHub editor is 127 chars wide
    - flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    # 21 flake8 testing of: saulpw/visidata...
    - git clone --depth=50 --branch=master saulpw/visidata ~/visidata
    - cd ~/visidata
    # stop the build if there are Python syntax errors or undefined names
    - flake8 . --count --select=E901,E999,F821,F822,F823 --statistics
    # exit-zero treates all errors as warnings.  The GitHub editor is 127 chars wide
    - flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    # 22 flake8 testing of: josephmisiti/awesome-machine-learning...
    - git clone --depth=50 --branch=master josephmisiti/awesome-machine-learning ~/awesome-machine-learning
    - cd ~/awesome-machine-learning
    # stop the build if there are Python syntax errors or undefined names
    - flake8 . --count --select=E901,E999,F821,F822,F823 --statistics
    # exit-zero treates all errors as warnings.  The GitHub editor is 127 chars wide
    - flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    # 23 flake8 testing of: cloudnativelabs/kube-shell...
    - git clone --depth=50 --branch=master cloudnativelabs/kube-shell ~/kube-shell
    - cd ~/kube-shell
    # stop the build if there are Python syntax errors or undefined names
    - flake8 . --count --select=E901,E999,F821,F822,F823 --statistics
    # exit-zero treates all errors as warnings.  The GitHub editor is 127 chars wide
    - flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    # 24 flake8 testing of: reiinakano/scikit-plot...
    - git clone --depth=50 --branch=master reiinakano/scikit-plot ~/scikit-plot
    - cd ~/scikit-plot
    # stop the build if there are Python syntax errors or undefined names
    - flake8 . --count --select=E901,E999,F821,F822,F823 --statistics
    # exit-zero treates all errors as warnings.  The GitHub editor is 127 chars wide
    - flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    # 25 flake8 testing of: fchollet/keras...
    - git clone --depth=50 --branch=master fchollet/keras ~/keras
    - cd ~/keras
    # stop the build if there are Python syntax errors or undefined names
    - flake8 . --count --select=E901,E999,F821,F822,F823 --statistics
    # exit-zero treates all errors as warnings.  The GitHub editor is 127 chars wide
    - flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
script: true
    # pytest --capture=sys
notifications:
    on_success: change
    on_failure: change  # `always` will be the setting once code changes slow down
