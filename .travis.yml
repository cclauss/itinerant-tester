group: travis_latest
language: python
env:
    - REPO=openai/EPG
    - REPO=openai/baselines  # has .travis.yml file
    - REPO=openai/deeptype
    - REPO=openai/finetune-transformer-lm
    - REPO=openai/generating-reviews-discovering-sentiment
    - REPO=openai/gradient-checkpointing  # has .travis.yml file
    - REPO=openai/gym  # has .travis.yml file
    - REPO=openai/gym-http-api  # has .travis.yml file
    - REPO=openai/gym-recording
    - REPO=openai/gym-soccer
    - REPO=openai/iaf
    - REPO=openai/improved-gan
    - REPO=openai/kubernetes-ec2-autoscaler  # has .travis.yml file
    - REPO=openai/maddpg
    - REPO=openai/mlsh
    - REPO=openai/mujoco-py  # has .travis.yml file
    - REPO=openai/multiagent-competition
    - REPO=openai/multiagent-particle-envs
    - REPO=openai/neural-gpu
    - REPO=openai/pixel-cnn
    - REPO=openai/retro  # has .travis.yml file
    - REPO=openai/retro-baselines
    - REPO=openai/retro-contest
    - REPO=openai/retro-movies
    - REPO=openai/roboschool
    - REPO=openai/robosumo
    - REPO=openai/sonic-on-ray
    - REPO=openai/universe  # has .travis.yml file
    - REPO=openai/universe-starter-agent
    - REPO=openai/weightnormcache: pip
python:
    - 2.7
    - 3.6
    #- nightly
    #- pypy
    #- pypy3
matrix:
    allow_failures:
        - python: 2.7
        - python: nightly
        - python: pypy
        - python: pypy3
install:
    #- pip install -r requirements.txt
    - pip install flake8  # pytest  # add other testing frameworks later
before_script:
    - URL=https://github.com/${REPO}
    - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
    - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
    - cd ~/${REPO}
script:
    - echo stop the build if there are Python syntax errors or undefined names
    - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
    - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
    - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
    - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
    #- true  # pytest --capture=sys
notifications:
    on_success: change
    on_failure: change  # `always` will be the setting once code changes slow down
