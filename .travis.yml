group: travis_latest
dist: xenial    # required for Python 3.7 (travis-ci/travis-ci#9069)
sudo: required  # required for Python 3.7 (travis-ci/travis-ci#9069)
language: python
cache: pip
python:
  #- 2.7
  #- 3.4
  #- 3.5
  #- 3.6
  - 3.7
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
    - REPO=GoogleCloudPlatform/PerfKitBenchmarker  # has .travis.yml file
    - REPO=GoogleCloudPlatform/appengine-guestbook-python
    - REPO=GoogleCloudPlatform/bigquery-bokeh-dashboard
    - REPO=GoogleCloudPlatform/cloud-dataproc
    - REPO=GoogleCloudPlatform/cloud-opensource-python  # has .travis.yml file
    - REPO=GoogleCloudPlatform/cloud-shell-tutorials
    - REPO=GoogleCloudPlatform/cloudml-samples
    - REPO=GoogleCloudPlatform/compute-image-packages  # has .travis.yml file
    - REPO=GoogleCloudPlatform/deploymentmanager-samples
    - REPO=GoogleCloudPlatform/distributed-load-testing-using-kubernetes
    - REPO=GoogleCloudPlatform/flask-talisman  # has .travis.yml file
    - REPO=GoogleCloudPlatform/forseti-security  # has .travis.yml file
    - REPO=GoogleCloudPlatform/getting-started-python  # has .travis.yml file
    - REPO=GoogleCloudPlatform/gke-datadog-demo
    - REPO=GoogleCloudPlatform/gke-logging-sinks-demo
    - REPO=GoogleCloudPlatform/gke-tracing-demo
    - REPO=GoogleCloudPlatform/google-auth-library-python  # has .travis.yml file
    - REPO=GoogleCloudPlatform/google-cloud-python
    - REPO=GoogleCloudPlatform/grpc-gcp-python
    - REPO=GoogleCloudPlatform/gsutil  # has .travis.yml file
    - REPO=GoogleCloudPlatform/healthcare
    - REPO=GoogleCloudPlatform/marketplace-k8s-app-tools  # has .travis.yml file
    - REPO=GoogleCloudPlatform/next18-ai-in-motion
    - REPO=GoogleCloudPlatform/professional-services
    - REPO=GoogleCloudPlatform/python-docs-samples  # has .travis.yml file
    - REPO=GoogleCloudPlatform/python-runtime  # has .travis.yml file
    - REPO=GoogleCloudPlatform/releasetool
    - REPO=GoogleCloudPlatform/repository-gardener  # has .travis.yml file
    - REPO=GoogleCloudPlatform/runtimes-common  # has .travis.yml file
    - REPO=GoogleCloudPlatform/synthtool
    
install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
