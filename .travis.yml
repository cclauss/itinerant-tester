group: travis_latest
dist: xenial  # required for Python >= 3.7 (travis-ci/travis-ci#9069)
language: python
cache: pip
python:
  #- 2.7
  #- 3.4
  #- 3.5
  #- 3.6
  - 3.7
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
  - REPO=hayd/Finisher
  - REPO=hayd/Python
  - REPO=hayd/SimpleCV  # has .travis.yml file
  - REPO=hayd/Zappa  # has .travis.yml file
  - REPO=hayd/caravel  # has .travis.yml file
  - REPO=hayd/contracts  # has .travis.yml file
  - REPO=hayd/ctox  # has .travis.yml file
  - REPO=hayd/docker-squash-1  # has .travis.yml file
  - REPO=hayd/electrum  # has .travis.yml file
  - REPO=hayd/flit  # has .travis.yml file
  - REPO=hayd/homu  # has .travis.yml file
  - REPO=hayd/inliner
  - REPO=hayd/leanpub
  - REPO=hayd/letsencrypt-aws  # has .travis.yml file
  - REPO=hayd/minigo
  - REPO=hayd/mocktest
  - REPO=hayd/moolah
  - REPO=hayd/ordered-set
  - REPO=hayd/pandas  # has .travis.yml file
  - REPO=hayd/pattern  # has .travis.yml file
  - REPO=hayd/pep8radius  # has .travis.yml file
  - REPO=hayd/ptpython  # has .travis.yml file
  - REPO=hayd/python-oauth2  # has .travis.yml file
  - REPO=hayd/rope  # has .travis.yml file
  - REPO=hayd/sns-to-slack
  - REPO=hayd/treq  # has .travis.yml file
  - REPO=hayd/vispy  # has .travis.yml file
  - REPO=hayd/watchdog  # has .travis.yml file
  - REPO=hayd/yahoo-finance
  - REPO=hayd/yapf  # has .travis.yml file
install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
