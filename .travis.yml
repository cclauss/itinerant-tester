group: travis_latest
dist: xenial    # required for Python 3.7 (travis-ci/travis-ci#9069)
sudo: required  # required for Python 3.7 (travis-ci/travis-ci#9069)
language: python
cache: pip
python:
  #- 2.7
  #- 3.4
  #- 3.5
  #- 3.6
  - 3.7
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
    - REPO=brettcannon/azure-kusto-python  # has .travis.yml file
    - REPO=brettcannon/bedevere  # has .travis.yml file
    - REPO=brettcannon/black  # has .travis.yml file
    - REPO=brettcannon/caniusepython3  # has .travis.yml file
    - REPO=brettcannon/conda-docs  # has .travis.yml file
    - REPO=brettcannon/core-workflow  # has .travis.yml file
    - REPO=brettcannon/cpython  # has .travis.yml file
    - REPO=brettcannon/django
    - REPO=brettcannon/flit  # has .travis.yml file
    - REPO=brettcannon/free-labour
    - REPO=brettcannon/gidgethub  # has .travis.yml file
    - REPO=brettcannon/github-bot-tutorial
    - REPO=brettcannon/jedi  # has .travis.yml file
    - REPO=brettcannon/modutil  # has .travis.yml file
    - REPO=brettcannon/oauthlib  # has .travis.yml file
    - REPO=brettcannon/packaging  # has .travis.yml file
    - REPO=brettcannon/pep425
    - REPO=brettcannon/peps  # has .travis.yml file
    - REPO=brettcannon/pipenv  # has .travis.yml file
    - REPO=brettcannon/pytest  # has .travis.yml file
    - REPO=brettcannon/python
    - REPO=brettcannon/python-language-server
    - REPO=brettcannon/python-packaging-user-guide
    - REPO=brettcannon/python-sample-pagination
    - REPO=brettcannon/sans-io  # has .travis.yml file
    - REPO=brettcannon/supporting-python-3  # has .travis.yml file
    - REPO=brettcannon/toga
    - REPO=brettcannon/unittest-xml-reporting  # has .travis.yml file
    - REPO=brettcannon/vibora
    - REPO=brettcannon/welcome-wagon-2018  # has .travis.yml file

install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
