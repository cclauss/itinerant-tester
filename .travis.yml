group: travis_latest
dist: xenial  # required for Python >= 3.7 (travis-ci/travis-ci#9069)
language: python
cache: pip
python:
  #- 2.7
  #- 3.4
  #- 3.5
  #- 3.6
  - 3.7
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
  - REPO=BookBrainz/CaliBBre
  - REPO=BookBrainz/bookbrainz-dev-docs
  - REPO=BookBrainz/bookbrainz-schema  # has .travis.yml file
  - REPO=BookBrainz/bookbrainz-ws  # has .travis.yml file
  - REPO=BookBrainz/python-bookbrainz  # has .travis.yml file
  - REPO=metabrainz/acousticbrainz-dataset-runner
  - REPO=metabrainz/acousticbrainz-server
  - REPO=metabrainz/bm
  - REPO=metabrainz/brainzbot-plugins  # has .travis.yml file
  - REPO=metabrainz/brainzutils-python  # has .travis.yml file
  - REPO=metabrainz/changed-mbid-feed
  - REPO=metabrainz/critiquebrainz
  - REPO=metabrainz/listenbrainz-recommendation-playground
  - REPO=metabrainz/listenbrainz-server  # has .travis.yml file
  - REPO=metabrainz/locustfiles
  - REPO=metabrainz/logster
  - REPO=metabrainz/mb-rngpy
  - REPO=metabrainz/mb2wikidatabot
  - REPO=metabrainz/mbs_logs_parsers
  - REPO=metabrainz/mbspotify
  - REPO=metabrainz/mbsssss
  - REPO=metabrainz/mbstats
  - REPO=metabrainz/mlhd-import
  - REPO=metabrainz/musicbrainz-logs
  - REPO=metabrainz/musicbrainz-server-log-analysis
  - REPO=metabrainz/newhost-replication-fix
  - REPO=metabrainz/nginx-upstream-manager  # has .travis.yml file
  - REPO=metabrainz/picard  # has .travis.yml file
  - REPO=metabrainz/picard-plugin-tools
  - REPO=metabrainz/picard-plugins
  - REPO=metabrainz/python-quickbooks  # has .travis.yml file
  - REPO=metabrainz/search-indexer
  - REPO=metabrainz/sir
  - REPO=metabrainz/sys-tools
  - REPO=metabrainz/xmpp-messaging-server

install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
