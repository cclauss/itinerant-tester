language: rust
env:
  - REPO=kyren/luster
  - REPO=wasmerio/ruby-ext-wasm
  - REPO=wasmerio/wasmer
  - REPO=jwilm/alacritty
  - REPO=dtolnay/select-rustc
  - REPO=rth/vtext
  - REPO=BurntSushi/ripgrep
  - REPO=sharkdp/bat
  - REPO=iceiix/stevenarella
  - REPO=vislyhq/stretch
  - REPO=ivanceras/sauron
  - REPO=rust-lang/rust
  - REPO=ggez/ggez
  - REPO=amethyst/rendy
  - REPO=xliiv/fui
  - REPO=valeriansaliou/sonic
  - REPO=tokio-rs/tokio
  - REPO=hecrj/coffee
  - REPO=sharkdp/fd
  - REPO=rayon-rs/rayon
  - REPO=rabite0/hunter
  - REPO=swc-project/swc
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "Testing of ${URL} on " ; rustc --version
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
