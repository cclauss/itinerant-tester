language: python
env:
    - REPO=rsms/interface
    - REPO=tensorflow/models
    - REPO=XX-net/XX-Net
    - REPO=scikit-learn/scikit-learn
    - REPO=pallets/flask
    - REPO=0x09AL/raven
    - REPO=deepmind/pysc2
    - REPO=concept-to-clinic/concept-to-clinic
    - REPO=scrapy/scrapy
    - REPO=PythonCharmers/python-future
    - REPO=google/protobuf
    - REPO=google/roboto
    - REPO=gevent/gevent
    - REPO=facebookresearch/ParlAI
    - REPO=getsentry/sentry
    - REPO=scrapy/scrapy
    - REPO=ArduPilot/pymavlink
    - REPO=ggtracker/sc2reader
python:
    - 2.7.13
    - 3.6
cache: pip
install:
    - pip install coveralls hacking pylint # pytest  # add other testing frameworks later
    - pip install pyflakes  # temporary because of pyflakes v1.6 changes at...
    # https://github.com/PyCQA/pyflakes/blob/master/NEWS.txt
before_script:
    - URL=https://github.com/${REPO}
    - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
    - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
    - cd ~/${REPO}
script:
    - echo stop the build if there are Python syntax errors or undefined names
    - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
    - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
    - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
    - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
after_success:
    - cd ~/${REPO}  # so pylint does not halt on invalid module name
    - touch __init__.py  # so that pylint does not claim to be lost
    - pylint .
    # - coveralls
notifications:
    on_success: change
on_failure: change  # `always` will be the setting once code changes slow down

