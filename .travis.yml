group: travis_latest
language: python
env:
    - REPO=spotify/crtauth  # has .travis.yml file
    - REPO=spotify/dataprocxy
    - REPO=spotify/dh-virtualenv  # has .travis.yml file
    - REPO=spotify/docker-stress
    - REPO=spotify/docker_interface  # has .travis.yml file
    - REPO=spotify/gcp-audit
    - REPO=spotify/gcp-firewall-enforcer
    - REPO=spotify/gordon  # has .travis.yml file
    - REPO=spotify/gordon-gcp  # has .travis.yml file
    - REPO=spotify/gordon-janitor  # has .travis.yml file
    - REPO=spotify/gordon-janitor-gcp  # has .travis.yml file
    - REPO=spotify/griffin  # has .travis.yml file
    - REPO=spotify/luigi  # has .travis.yml file
    - REPO=spotify/mlockexec
    - REPO=spotify/postgresql-metrics
    - REPO=spotify/pyeos
    - REPO=spotify/pyfg
    - REPO=spotify/pyschema
    - REPO=spotify/python-graphwalker  # has .travis.yml file
    - REPO=spotify/pythonflow  # has .travis.yml file
    - REPO=spotify/requests-crtauth  # has .travis.yml file
    - REPO=spotify/shumway  # has .travis.yml file
    - REPO=spotify/snakebite  # has .travis.yml file
    - REPO=spotify/sparkey-python
    - REPO=spotify/spotify-tensorflow
    - REPO=spotify/spymaster
    - REPO=spotify/super-smash-brogp
    - REPO=spotify/testing-game
    - REPO=spotify/ulogger  # has .travis.yml file
    - REPO=spotify/vdisk
cache: pip
python:
    #- 2.7
    - 3.6
    #- nightly
    #- pypy
    #- pypy3
matrix:
    allow_failures:
        - python: nightly
        - python: pypy
        - python: pypy3
install:
    #- pip install -r requirements.txt
    - pip install flake8  # pytest  # add other testing frameworks later
before_script:
    - URL=https://github.com/${REPO}
    - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
    - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
    - cd ~/${REPO}
script:
    - echo stop the build if there are Python syntax errors or undefined names
    - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
    - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
    - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
    - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
    #- true  # pytest --capture=sys
notifications:
    on_success: change
    on_failure: change  # `always` will be the setting once code changes slow down
