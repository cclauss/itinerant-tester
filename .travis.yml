group: travis_latest
dist: xenial  # required for Python >= 3.7 (travis-ci/travis-ci#9069)
language: python
cache: pip
python:
  #- 2.7
  #- 3.4
  #- 3.5
  #- 3.6
  - 3.7
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
  - REPO=edx/code-annotations  # has .travis.yml file
  - REPO=edx/configuration  # has .travis.yml file
  - REPO=edx/course-discovery  # has .travis.yml file
  - REPO=edx/credentials  # has .travis.yml file
  - REPO=edx/django-oauth2-provider  # has .travis.yml file
  - REPO=edx/ecommerce  # has .travis.yml file
  - REPO=edx/ecommerce-worker  # has .travis.yml file
  - REPO=edx/edx-analytics-data-api  # has .travis.yml file
  - REPO=edx/edx-analytics-pipeline  # has .travis.yml file
  - REPO=edx/edx-e2e-tests  # has .travis.yml file
  - REPO=edx/edx-enterprise  # has .travis.yml file
  - REPO=edx/edx-enterprise-data  # has .travis.yml file
  - REPO=edx/edx-gomatic  # has .travis.yml file
  - REPO=edx/edx-notes-api  # has .travis.yml file
  - REPO=edx/edx-ora2  # has .travis.yml file
  - REPO=edx/edx-platform
  - REPO=edx/edx-proctoring  # has .travis.yml file
  - REPO=edx/edx-submissions  # has .travis.yml file
  - REPO=edx/edx-val  # has .travis.yml file
  - REPO=edx/edx-video-pipeline  # has .travis.yml file
  - REPO=edx/edx-video-worker  # has .travis.yml file
  - REPO=edx/mdrst
  - REPO=edx/notifier  # has .travis.yml file
  - REPO=edx/opaque-keys  # has .travis.yml file
  - REPO=edx/openedx-census
  - REPO=edx/repo-tools  # has .travis.yml file
  - REPO=edx/testeng-ci  # has .travis.yml file
  - REPO=edx/tubular  # has .travis.yml file
  - REPO=edx/xblock-lti-consumer  # has .travis.yml file
  - REPO=edx/xqueue  # has .travis.yml file
  
install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
