group: travis_latest
dist: xenial  # required for Python >= 3.7 (travis-ci/travis-ci#9069)
language: python
cache: pip
python:
  #- 2.7
  #- 3.5
  #- 3.6
  - 3.7
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
  - REPO=momosecurity/aswan
  - REPO=donnemartin/system-design-primer
  - REPO=ansible/awx
  - REPO=Azure/azure-cli
  - REPO=CorentinJ/Real-Time-Voice-Cloning
  - REPO=fbsamples/fbctf-2019-challenges
  - REPO=realsirjoe/instagram-scraper
  - REPO=NVlabs/stylegan
  - REPO=open-mmlab/mmdetection
  - REPO=Cyash/Pothole_Detection
  - REPO=Zucccs/PhoneSploit
  - REPO=pandas-profiling/pandas-profiling
  - REPO=home-assistant/home-assistant
  - REPO=dask/dask
  - REPO=mbadry1/DeepLearning.ai-Summary
  - REPO=donnemartin/interactive-coding-challenges
  - REPO=pydanny/cookiecutter-django
  - REPO=localstack/localstack
  - REPO=prometheus/client_python
  - REPO=rwv/chinese-dos-games
  - REPO=SlavikMIPT/tgcloud
  - REPO=amir-abdi/keras_to_tensorflow
  - REPO=dmlc/tvm
  - REPO=sergiomsilva/alpr-unconstrained
  - REPO=Alivon/Panda-Learning
  - REPO=n1nj4sec/pupy
  - REPO=PythonCharmers/python-future
  - REPO=ansible/ansible
  - REPO=oaubert/python-vlc
  - REPO=ColdGrub1384/Pyto
  - REPO=getsentry/sentry
  - REPO=CoreSecurity/impacket
  - REPO=nodejs/node-gyp
  - REPO=internetarchive/openlibrary
  - REPO=google/ffn
  - REPO=webpy/webpy
  - REPO=ibm-watson-iot/connector-cloudant
  - REPO=ibm-watson-iot/device-kodi
  - REPO=apache/beam
  - REPO=apache/incubator-mxnet
  - REPO=hyperledger/iroha-python
  - REPO=Supervisor/supervisor
  - REPO=ibm-watson-iot/functions
  - REPO=apache/spark
  - REPO=Tribler/tribler
  - REPO=nodejs/node
  - REPO=ArduPilot/ardupilot
  - REPO=ArduPilot/pymavlink
  - REPO=dronekit/dronekit-python
  - REPO=ansible/awx
  - REPO=cheshirekow/cmake_format
  - REPO=matplotlib/matplotlib
  - REPO=ckan/ckan
  - REPO=ggtracker/sc2reader
  - REPO=httplib2/httplib2
  - REPO=hyperledger/fabric-sdk-py
install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E9,F63,F72,F82 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
