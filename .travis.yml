group: travis_latest
dist: xenial    # required for Python >= 3.7 (travis-ci/travis-ci#9069)
sudo: required  # required for Python >= 3.7 (travis-ci/travis-ci#9069)
language: python
cache: pip
python:
  #- 2.7
  #- 3.4
  #- 3.5
  #- 3.6
  - 3.7
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
    - REPO=abbeyokgo/-
    - REPO=abbeyokgo/Bilibili_login_geetest
    - REPO=abbeyokgo/FreeForWenku
    - REPO=abbeyokgo/MachineLearning_Python
    - REPO=abbeyokgo/ProxyPool
    - REPO=abbeyokgo/PyOne
    - REPO=abbeyokgo/SinaWeibo
    - REPO=abbeyokgo/aria2c-py
    - REPO=abbeyokgo/blog_migrate_tool
    - REPO=abbeyokgo/crawler-py
    - REPO=abbeyokgo/douyu_dm
    - REPO=abbeyokgo/get-email-by-tieba
    - REPO=abbeyokgo/hexo_weibo_image
    - REPO=abbeyokgo/k1kmz
    - REPO=abbeyokgo/ojbk_jiexi
    - REPO=abbeyokgo/owllook
    - REPO=abbeyokgo/payjs_faka
    - REPO=abbeyokgo/proxy_pool  # has .travis.yml file
    - REPO=abbeyokgo/python-bittrex  # has .travis.yml file
    - REPO=abbeyokgo/python-flickr
    - REPO=abbeyokgo/python-spider
    - REPO=abbeyokgo/pythoncode
    - REPO=abbeyokgo/pytumblr  # has .travis.yml file
    - REPO=abbeyokgo/tumblr_crawler
    - REPO=abbeyokgo/wechat_articles_spider
    - REPO=abbeyokgo/wechat_jump_game
    - REPO=abbeyokgo/weiboPicDownloader
    - REPO=abbeyokgo/youku
    - REPO=abbeyokgo/youtube-upload

install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
