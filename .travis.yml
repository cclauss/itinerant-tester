group: travis_latest
dist: xenial    # required for Python 3.7 (travis-ci/travis-ci#9069)
sudo: required  # required for Python 3.7 (travis-ci/travis-ci#9069)
language: python
cache: pip
python:
  #- 2.7
  #- 3.4
  #- 3.5
  #- 3.6
  - 3.7
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
    - REPO=aaronpenne/data_visualization
    - REPO=aaronpenne/generative_art
    - REPO=aaronpenne/get_noaa_ghcn_data
    - REPO=aaronpenne/government_corpus
    - REPO=aaronpenne/hangouts_history_parser
    - REPO=aaronpenne/somafm_charts
    - REPO=adrienverge/PhotoCollage  # has .travis.yml file
    - REPO=adrienverge/ansible
    - REPO=adrienverge/check-all-the-things
    - REPO=adrienverge/cinder
    - REPO=adrienverge/cli2man
    - REPO=adrienverge/coala-bears  # has .travis.yml file
    - REPO=adrienverge/context_unnester
    - REPO=adrienverge/coucharchive  # has .travis.yml file
    - REPO=adrienverge/couchdb-documentation  # has .travis.yml file
    - REPO=adrienverge/couchdb-python  # has .travis.yml file
    - REPO=adrienverge/familytreemaker
    - REPO=adrienverge/git-lint  # has .travis.yml file
    - REPO=adrienverge/hachiko
    - REPO=adrienverge/heat
    - REPO=adrienverge/horizon
    - REPO=adrienverge/keyboard-stickers
    - REPO=adrienverge/kijijiapi
    - REPO=adrienverge/localstripe  # has .travis.yml file
    - REPO=adrienverge/nest-css
    - REPO=adrienverge/python-path-specification
    - REPO=adrienverge/python-saharaclient
    - REPO=adrienverge/rhythmbox_playlist_to_m3u
    - REPO=adrienverge/sahara
    - REPO=adrienverge/sahara-specs
    - REPO=adrienverge/yamllint  # has .travis.yml file

install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
