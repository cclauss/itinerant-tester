group: travis_latest
dist: xenial  # required for Python >= 3.7 (travis-ci/travis-ci#9069)
language: python
cache: pip
python:
  #- 2.7
  #- 3.4
  #- 3.5
  #- 3.6
  - 3.7
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
  - REPO=davidtavarez/APILoginBruteForce
  - REPO=davidtavarez/BeaconGraph
  - REPO=davidtavarez/IronPentest
  - REPO=davidtavarez/PassGen
  - REPO=davidtavarez/SILENTTRINITY
  - REPO=davidtavarez/WebMap
  - REPO=davidtavarez/glucolog  # has .travis.yml file
  - REPO=davidtavarez/jswc
  - REPO=davidtavarez/linux-soft-exploit-suggester
  - REPO=davidtavarez/masscan-ui
  - REPO=davidtavarez/pwndb
  - REPO=davidtavarez/python-prompt-toolkit  # has .travis.yml file
  - REPO=davidtavarez/pythonsti007
  - REPO=davidtavarez/weblocator
  - REPO=D4Vinci/AdflyUrlGrabber
  - REPO=D4Vinci/AirCracker
  - REPO=D4Vinci/Anti_Killer
  - REPO=D4Vinci/Arr4ng3d_Sh4r3
  - REPO=D4Vinci/Chrome-Extractor
  - REPO=D4Vinci/Clickjacking-Tester
  - REPO=D4Vinci/Cr3dOv3r
  - REPO=D4Vinci/Cuteit
  - REPO=D4Vinci/Domain-Checker
  - REPO=D4Vinci/Dr0p1t-Framework
  - REPO=D4Vinci/Exif-Grabber
  - REPO=D4Vinci/Humax-CLI
  - REPO=D4Vinci/Insta-Crawler
  - REPO=D4Vinci/My-laziness
  - REPO=D4Vinci/One-Lin3r
  - REPO=D4Vinci/Palsy-Virus
  - REPO=D4Vinci/Paste2Web
  - REPO=D4Vinci/PasteJacker
  - REPO=D4Vinci/PyFlooder
  - REPO=D4Vinci/PyLoggy
  - REPO=D4Vinci/QRLJacking
  - REPO=D4Vinci/ReverseHttp
  - REPO=D4Vinci/Scripts
  - REPO=D4Vinci/Sublist3r
  - REPO=D4Vinci/Twitter-Info
  - REPO=D4Vinci/WifiPass
  - REPO=D4Vinci/Wormy
  - REPO=D4Vinci/XOE
  - REPO=D4Vinci/elpscrk
  - REPO=D4Vinci/pwndb
install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
