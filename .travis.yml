group: travis_latest
# dist: xenial  # required for Python 3.7
# sudo: true    # required for Python 3.7
language: python
env:
    - REPO=llSourcell/A-Guide-to-DeepMinds-StarCraft-AI-Environment
    - REPO=llSourcell/AI_For_Music_Composition
    - REPO=llSourcell/AI_for_Financial_Data
    - REPO=llSourcell/AI_for_Resumes
    - REPO=llSourcell/AI_in_Medicine_Clinical_Imaging_Classification
    - REPO=llSourcell/Deep_Learning_with_Intel
    - REPO=llSourcell/Generative-Adversarial-Network-Demo
    - REPO=llSourcell/Generative-Query-Network
    - REPO=llSourcell/HashGraph_Explained
    - REPO=llSourcell/Landing-a-SpaceX-Falcon-Heavy-Rocket
    - REPO=llSourcell/Make_Money_with_Tensorflow
    - REPO=llSourcell/Neural_Network_Voices
    - REPO=llSourcell/Pokemon_GAN
    - REPO=llSourcell/Reinforcement_Learning_for_Stock_Prediction
    - REPO=llSourcell/Research_to_Code
    - REPO=llSourcell/backpropagation_explained
    - REPO=llSourcell/capsule_networks
    - REPO=llSourcell/chatbot_tutorial
    - REPO=llSourcell/genetic_algorithm_challenge
    - REPO=llSourcell/keras  # has .travis.yml file
    - REPO=llSourcell/learn_math_fast
    - REPO=llSourcell/linear_regression_live
    - REPO=llSourcell/new_repository
    - REPO=llSourcell/q_learning_demo
    - REPO=llSourcell/self_driving_cars_explained
    - REPO=llSourcell/tensorflow_chatbot
    - REPO=llSourcell/tensorflow_demo
    - REPO=llSourcell/twitter_sentiment_challenge
    - REPO=llSourcell/visualize_dataset_demo
    - REPO=llSourcell/world_models

cache: pip
python:
    #- 2.7
    - 3.6
    #- nightly
    #- pypy
    #- pypy3
matrix:
    allow_failures:
        - 2.7
        - python: nightly
        - python: pypy
        - python: pypy3
install:
    #- pip install -r requirements.txt
    - pip install flake8  # pytest  # add other testing frameworks later
before_script:
    - URL=https://github.com/${REPO}
    - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
    - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
    - cd ~/${REPO}
script:
    - echo stop the build if there are Python syntax errors or undefined names
    - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
    - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
    - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
    - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
    #- true  # pytest --capture=sys
notifications:
    on_success: change
    on_failure: change  # `always` will be the setting once code changes slow down
