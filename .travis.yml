group: travis_latest
dist: xenial  # required for Python >= 3.7 (travis-ci/travis-ci#9069)
language: python
cache: pip
python:
  #- 2.7
  #- 3.5
  #- 3.6
  - 3.7
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
  - REPO=maxnoe/HoppladiHopplada
  - REPO=maxnoe/adventofcode2017
  - REPO=maxnoe/astericsschool18-gitdemo
  - REPO=maxnoe/astropy  # has .travis.yml file
  - REPO=maxnoe/cpython  # has .travis.yml file
  - REPO=maxnoe/ctapipe  # has .travis.yml file
  - REPO=maxnoe/ctapipe-extra
  - REPO=maxnoe/cython-demo
  - REPO=maxnoe/dask-drmaa  # has .travis.yml file
  - REPO=maxnoe/drmaa-python  # has .travis.yml file
  - REPO=maxnoe/emcee  # has .travis.yml file
  - REPO=maxnoe/fontfinder  # has .travis.yml file
  - REPO=maxnoe/funfolding  # has .travis.yml file
  - REPO=maxnoe/git_demo_20180528  # has .travis.yml file
  - REPO=maxnoe/gridmap  # has .travis.yml file
  - REPO=maxnoe/matplotlib  # has .travis.yml file
  - REPO=maxnoe/mensabot
  - REPO=maxnoe/obelics-git-demo  # has .travis.yml file
  - REPO=maxnoe/peewee  # has .travis.yml file
  - REPO=maxnoe/pizza2
  - REPO=maxnoe/plz_plot
  - REPO=maxnoe/pyhexgrid
  - REPO=maxnoe/python-plotting
  - REPO=maxnoe/python-solarsystem
  - REPO=maxnoe/python_package_template  # has .travis.yml file
  - REPO=maxnoe/restructuredtext-lint  # has .travis.yml file
  - REPO=maxnoe/scikit-learn  # has .travis.yml file
  - REPO=maxnoe/texlive-batch-installation  # has .travis.yml file
  - REPO=maxnoe/toolbox2018-git-demo
  - REPO=maxnoe/vim-snippets  # has .travis.yml file
  - REPO=maxnoe/who-dropped-python2

install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=4.x
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E9,F63,F72,F82 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
