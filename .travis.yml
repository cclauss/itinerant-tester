group: travis_latest
dist: bionic
language: python
cache: pip
python:
  #- 2.7
  #- 3.5
  #- 3.6
  #- 3.7
  - 3.8
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
  - REPO=eriklindernoren/ML-From-Scratch
  - REPO=testerSunshine/12306
  - REPO=521xueweihan/HelloGitHub
  - REPO=shengqiangzhang/examples-of-web-crawlers
  - REPO=f-prime/arcanevm
  - REPO=facebookresearch/detectron2
  - REPO=zhaipro/easy12306
  - REPO=YinAoXiong/12306_code_server
  - REPO=home-assistant/home-assistant
  - REPO=hunters-forge/ThreatHunter-Playbook
  - REPO=craigsdennis/intro-to-apis-course
  - REPO=DropsDevopsOrg/ECommerceCrawlers
  - REPO=google-research/ALBERT
  - REPO=Tib3rius/AutoRecon
  - REPO=xHak9x/fbi
  - REPO=cs230-stanford/cs230-code-examples
  - REPO=wkentaro/labelme
  - REPO=apache/incubator-mxnet
  - REPO=aws/sagemaker-python-sdk
  - REPO=microsoft/c9-python-getting-started
  - REPO=oppia/oppia
  - REPO=Avik-Jain/100-Days-Of-ML-Code
  - REPO=minimaxir/gpt-2-simple
  - REPO=astropy/astropy
  - REPO=matrix-org/synapse
  - REPO=n1nj4sec/pupy
  - REPO=PythonCharmers/python-future
  - REPO=ansible/ansible
  - REPO=oaubert/python-vlc
  - REPO=ColdGrub1384/Pyto
  - REPO=getsentry/sentry
  - REPO=CoreSecurity/impacket
  - REPO=internetarchive/openlibrary
  - REPO=google/ffn
  - REPO=v8/v8
  - REPO=ibm-watson-iot/connector-cloudant
  - REPO=ibm-watson-iot/device-kodi
  - REPO=apache/beam
  - REPO=apache/incubator-mxnet
  - REPO=hyperledger/iroha-python
  - REPO=Supervisor/supervisor
  - REPO=ibm-watson-iot/functions
  - REPO=apache/spark
  - REPO=Tribler/tribler
  - REPO=webpy/webpy
  - REPO=nodejs/node
  - REPO=nodejs/node-gyp
  - REPO=ArduPilot/ardupilot
  - REPO=ArduPilot/pymavlink
  - REPO=dronekit/dronekit-python
  - REPO=ansible/awx
  - REPO=cheshirekow/cmake_format
  - REPO=matplotlib/matplotlib
  - REPO=ckan/ckan
  - REPO=ggtracker/sc2reader
  - REPO=httplib2/httplib2
  - REPO=hyperledger/fabric-sdk-py
install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
