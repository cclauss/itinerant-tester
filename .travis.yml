group: travis_latest
dist: xenial    # required for Python 3.7 (travis-ci/travis-ci#9069)
sudo: required  # required for Python 3.7 (travis-ci/travis-ci#9069)
language: python
cache: pip
python:
  #- 2.7
  #- 3.4
  #- 3.5
  #- 3.6
  - 3.7
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
    - REPO=IBM/AIF360  # has .travis.yml file
    - REPO=IBM/BAGAN
    - REPO=IBM/CLEVER-Robustness-Score
    - REPO=IBM/DLaaS-Getting-StartedTutorial
    - REPO=IBM/MAX-Audio-Classifier  # has .travis.yml file
    - REPO=IBM/MAX-Breast-Cancer-Mitosis-Detector  # has .travis.yml file
    - REPO=IBM/MAX-Fast-Neural-Style-Transfer
    - REPO=IBM/MAX-Image-Caption-Generator  # has .travis.yml file
    - REPO=IBM/MAX-Image-Colorizer  # has .travis.yml file
    - REPO=IBM/MAX-Inception-ResNet-v2  # has .travis.yml file
    - REPO=IBM/MAX-News-Text-Generator
    - REPO=IBM/MAX-Object-Detector  # has .travis.yml file
    - REPO=IBM/MAX-Scene-Classifier  # has .travis.yml file
    - REPO=IBM/MAX-Sports-Video-Classifier  # has .travis.yml file
    - REPO=IBM/MAX-Weather-Forecaster
    - REPO=IBM/MAX-Word-Embedding-Generator
    - REPO=IBM/Portfolio-Optimize  # has .travis.yml file
    - REPO=IBM/Semantic-Search-for-Sustainable-Development
    - REPO=IBM/ansible-role-infosvr-import-export
    - REPO=IBM/gan-toolkit
    - REPO=IBM/microprobe  # has .travis.yml file
    - REPO=IBM/ny-power  # has .travis.yml file
    - REPO=IBM/pytorch-seq2seq  # has .travis.yml file
    - REPO=IBM/quickcd
    - REPO=IBM/rl-testbed-for-energyplus
    - REPO=IBM/secure-bitcoin-wallet
    - REPO=IBM/stix-shifter  # has .travis.yml file
    - REPO=IBM/watson-assistant-workbench  # has .travis.yml file
    - REPO=IBM/wc-devops-utilities
    - REPO=IBM/worklog
    
install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
