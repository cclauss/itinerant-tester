group: travis_latest
dist: xenial  # required for Python >= 3.7 (travis-ci/travis-ci#9069)
language: python
cache: pip
python:
  #- 2.7
  #- 3.4
  #- 3.5
  #- 3.6
  - 3.7
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
    - REPO=IBM/AIF360  # has .travis.yml file
    - REPO=IBM/CROWN-Robustness-Certification
    - REPO=IBM/MAX-Facial-Age-Estimator-Web-App  # has .travis.yml file
    - REPO=IBM/MAX-Framework
    - REPO=IBM/MAX-Review-Text-Generator  # has .travis.yml file
    - REPO=IBM/MAX-Scene-Classifier  # has .travis.yml file
    - REPO=IBM/Portfolio-Optimize  # has .travis.yml file
    - REPO=IBM/Portfolio-compute  # has .travis.yml file
    - REPO=IBM/Predictive-Market-Stress-Testing  # has .travis.yml file
    - REPO=IBM/audioset-classification
    - REPO=IBM/cognitive-moderator-service
    - REPO=IBM/connector-for-storediq
    - REPO=IBM/dsrc
    - REPO=IBM/home-assistant
    - REPO=IBM/iot-device-trend-analysis  # has .travis.yml file
    - REPO=IBM/max-tutorial-app-python
    - REPO=IBM/mi-prometheus
    - REPO=IBM/nlc-icd10-classifier  # has .travis.yml file
    - REPO=IBM/ny-power  # has .travis.yml file
    - REPO=IBM/osslm-ansible-resource-manager
    - REPO=IBM/page-lab
    - REPO=IBM/photorealistic-blocksworld
    - REPO=IBM/powerai-transfer-learning
    - REPO=IBM/pytorch-seq2seq  # has .travis.yml file
    - REPO=IBM/serverless-app-python
    - REPO=IBM/solution-test-samples
    - REPO=IBM/stix-shifter  # has .travis.yml file
    - REPO=IBM/tensorflow-hangul-recognition  # has .travis.yml file
    - REPO=IBM/watson-online-store  # has .travis.yml file
    - REPO=IBM/wc-devops-utilities

install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
