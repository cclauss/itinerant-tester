group: travis_latest
dist: xenial    # required for Python 3.7 (travis-ci/travis-ci#9069)
sudo: required  # required for Python 3.7 (travis-ci/travis-ci#9069)
language: python
cache: pip
python:
  #- 2.7
  #- 3.4
  #- 3.5
  #- 3.6
  - 3.7
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
    - REPO=asottile/PyGithub  # has .travis.yml file
    - REPO=asottile/add-trailing-comma  # has .travis.yml file
    - REPO=asottile/all-repos  # has .travis.yml file
    - REPO=asottile/all-repos-depends  # has .travis.yml file
    - REPO=asottile/asottile.github.io
    - REPO=asottile/aspy.refactor_imports  # has .travis.yml file
    - REPO=asottile/aspy.yaml  # has .travis.yml file
    - REPO=asottile/astpretty  # has .travis.yml file
    - REPO=asottile/aws-cli  # has .travis.yml file
    - REPO=asottile/blacken-docs  # has .travis.yml file
    - REPO=asottile/color-code  # has .travis.yml file
    - REPO=asottile/docker-py  # has .travis.yml file
    - REPO=asottile/future-fstrings  # has .travis.yml file
    - REPO=asottile/git-code-debt  # has .travis.yml file
    - REPO=asottile/gitlint  # has .travis.yml file
    - REPO=asottile/ircbot
    - REPO=asottile/mypy  # has .travis.yml file
    - REPO=asottile/pleasant
    - REPO=asottile/pyterminalsize  # has .travis.yml file
    - REPO=asottile/python-modernize  # has .travis.yml file
    - REPO=asottile/pythoncapi
    - REPO=asottile/pyupgrade  # has .travis.yml file
    - REPO=asottile/reorder_python_imports  # has .travis.yml file
    - REPO=asottile/scratch
    - REPO=asottile/seed-isort-config  # has .travis.yml file
    - REPO=asottile/setuptools-golang  # has .travis.yml file
    - REPO=asottile/tox-virtualenv-no-download  # has .travis.yml file
    - REPO=asottile/unknown-horizons  # has .travis.yml file
    - REPO=asottile/watch-plz  # has .travis.yml file
    - REPO=asottile/yesqa  # has .travis.yml file
  
install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
