group: travis_latest
dist: xenial    # required for Python >= 3.7 (travis-ci/travis-ci#9069)
sudo: required  # required for Python >= 3.7 (travis-ci/travis-ci#9069)
language: python
cache: pip
python:
  #- 2.7
  #- 3.4
  #- 3.5
  #- 3.6
  - 3.7
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
    - REPO=jazzband/contextlib2  # has .travis.yml file
    - REPO=jazzband/django-admin2  # has .travis.yml file
    - REPO=jazzband/django-analytical  # has .travis.yml file
    - REPO=jazzband/django-authority  # has .travis.yml file
    - REPO=jazzband/django-axes  # has .travis.yml file
    - REPO=jazzband/django-configurations  # has .travis.yml file
    - REPO=jazzband/django-constance  # has .travis.yml file
    - REPO=jazzband/django-debug-toolbar  # has .travis.yml file
    - REPO=jazzband/django-discover-jenkins  # has .travis.yml file
    - REPO=jazzband/django-embed-video  # has .travis.yml file
    - REPO=jazzband/django-flatblocks  # has .travis.yml file
    - REPO=jazzband/django-hosts  # has .travis.yml file
    - REPO=jazzband/django-ical  # has .travis.yml file
    - REPO=jazzband/django-model-utils  # has .travis.yml file
    - REPO=jazzband/django-mongonaut  # has .travis.yml file
    - REPO=jazzband/django-oauth-toolkit  # has .travis.yml file
    - REPO=jazzband/django-pipeline  # has .travis.yml file
    - REPO=jazzband/django-push-notifications  # has .travis.yml file
    - REPO=jazzband/django-queued-storage  # has .travis.yml file
    - REPO=jazzband/django-redshift-backend  # has .travis.yml file
    - REPO=jazzband/django-robots  # has .travis.yml file
    - REPO=jazzband/django-silk  # has .travis.yml file
    - REPO=jazzband/django-simple-menu  # has .travis.yml file
    - REPO=jazzband/django-sorter  # has .travis.yml file
    - REPO=jazzband/django-widget-tweaks  # has .travis.yml file
    - REPO=jazzband/inflect  # has .travis.yml file
    - REPO=jazzband/pip-tools  # has .travis.yml file
    - REPO=jazzband/prettytable
    - REPO=jazzband/sorl-thumbnail  # has .travis.yml file
    - REPO=jazzband/website

install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
