sudo: false
dist: trusty
language: python
env:
    - REPO=tensorflow/models
    - REPO=reinforceio/tensorforce
    - REPO=astorfi/TensorFlow-World-Resources
    - REPO=fchollet/keras
    - REPO=nccgroup/Winpayloads
    - REPO=Kaixhin/NoisyNet-A3C
    - REPO=DarkSand/Sasila
    - REPO=AlexiaJM/Deep-learning-with-cats
    - REPO=soimort/you-get
    - REPO=pallets/flask
    - REPO=josephmisiti/awesome-machine-learning
    - REPO=GirlsFirst/SIP-2017-starter
    - REPO=requests/requests
    - REPO=jakubroztocil/httpie
    - REPO=https://httpie.org
python:
    - 2.7.13
    - 3.6.1
install:
    - pip install flake8  # pytest  # add some test later
script:
    - URL=https://github.com/${REPO}
    - git clone --depth=50 --branch=master ${URL} ~/${REPO}
    - cd ~/${REPO}
    - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
    - echo stop the build if there are Python syntax errors or undefined names
    - flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
    - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
    - flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
notifications:
    on_success: change
    on_failure: change  # `always` will be the setting once code changes slow down
