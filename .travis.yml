group: travis_latest
dist: xenial  # required for Python >= 3.7 (travis-ci/travis-ci#9069)
language: python
cache: pip
python:
  #- 2.7
  #- 3.4
  #- 3.5
  #- 3.6
  - 3.7
matrix:
  allow_failures:
    - python: 2.7
    - python: nightly
    - python: pypy
    - python: pypy3
env:
  - REPO=chdoig/Metrics  # has .travis.yml file
  - REPO=chdoig/Smashfast
  - REPO=chdoig/blaze  # has .travis.yml file
  - REPO=chdoig/conda-docs
  - REPO=chdoig/conda-env
  - REPO=chdoig/elasticsearch-py  # has .travis.yml file
  - REPO=chdoig/my-tests
  - REPO=chdoig/odo  # has .travis.yml file
  - REPO=chdoig/pyDojos
  - REPO=chdoig/pycon2015-kaggle-tutorial
  - REPO=chdoig/refinery
  - REPO=chdoig/scipy2015-blaze-bokeh
  - REPO=mrocklin/autograd  # has .travis.yml file
  - REPO=mrocklin/binder-bokeh-server
  - REPO=mrocklin/conda-pack  # has .travis.yml file
  - REPO=mrocklin/cupy  # has .travis.yml file
  - REPO=mrocklin/dask  # has .travis.yml file
  - REPO=mrocklin/dask-cuda
  - REPO=mrocklin/dask-cudf  # has .travis.yml file
  - REPO=mrocklin/dask-ec2  # has .travis.yml file
  - REPO=mrocklin/dask-geopandas
  - REPO=mrocklin/dask-jobqueue  # has .travis.yml file
  - REPO=mrocklin/dask-kubernetes
  - REPO=mrocklin/dask-ml  # has .travis.yml file
  - REPO=mrocklin/dask-mpi
  - REPO=mrocklin/dask-xgboost
  - REPO=mrocklin/distributed  # has .travis.yml file
  - REPO=mrocklin/distributed-feedstock  # has .travis.yml file
  - REPO=mrocklin/gcsfs  # has .travis.yml file
  - REPO=mrocklin/joblib  # has .travis.yml file
  - REPO=mrocklin/multipledispatch  # has .travis.yml file
  - REPO=mrocklin/pandas  # has .travis.yml file
  - REPO=mrocklin/py32test
  - REPO=mrocklin/skein  # has .travis.yml file
  - REPO=mrocklin/slides
  - REPO=mrocklin/sparse  # has .travis.yml file
  - REPO=mrocklin/symbolic-array
  - REPO=mrocklin/tangent  # has .travis.yml file
  - REPO=mrocklin/tpot  # has .travis.yml file
  - REPO=mrocklin/tutorials
  - REPO=mrocklin/xarray  # has .travis.yml file
  - REPO=mrocklin/zero-to-jupyterhub-k8s  # has .travis.yml file
install:
  #- pip install -r requirements.txt
  - pip install flake8  # pytest  # add other testing frameworks later
before_script:
  - URL=https://github.com/${REPO}
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
  - cd ~/${REPO}
script:
  - echo stop the build if there are Python syntax errors or undefined names
  - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
  - time flake8 . --count --select=E9,F63,F72,F82 --show-source --statistics
  - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
  - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #- true  # pytest --capture=sys
notifications:
  on_success: change
  on_failure: change  # `always` will be the setting once code changes slow down
